[2026-08-30 00:00] Handover-ready experiment and evaluation workflow

Request summary:
- Audit and update the README for handover.
- Maintain every experiment track eventually, but begin with the EEG alignment experiment defined by the existing alignment configs.
- Make the EEG alignment experiment work end-to-end from a simple local shell launcher that trains one experiment and then invokes a separate evaluation script.
- Remove SLURM-specific launch complexity from the supported workflow; revisit a minimal wrapper only after the local workflow works.
- Keep TensorBoard training-specific. Evaluation must produce reproducible tabular results (CSV, with machine-readable JSON if useful), saved alongside the selected checkpoint and evaluation metadata.
- Remove deprecated notebooks and scripts after inventorying their functionality and migrating or explicitly recording anything still needed.
- Break the work into verifiable subtasks and document how reported metrics can be recreated.

Implementation steps:
1. Establish the EEG alignment handover surface from the existing `train_eeg_align.yaml`, `train_eeg.py`, and `test_eeg.py` path. Verify prerequisites, output locations, checkpoint selection, and the smallest runnable dataset/model configuration. Completed: the config explicitly selects NICE with alignment enabled and both reconstruction flags disabled.
2. Audit the existing experiment launch scripts before changing or removing them. `scripts/evaluation/run_experiment.sh` already implements train-then-evaluate, while `launch_local.sh` implements local execution around a parameter-file task. Retain reusable behavior and separate the useful single-run flow from sweep/SLURM orchestration instead of deleting these scripts speculatively.
3. Add a minimal EEG alignment experiment config and one local launcher, reusing the existing `run_experiment.sh` behavior where practical. Remove its mandatory Singularity dependency, generic script positional arguments, environment-specific run naming, and fragile argument expansion from the handover path. The launcher must locate the exact produced run, invoke the separate evaluator, and print paths to the checkpoint, training TensorBoard directory, evaluation CSV/YAML, and reconstructions. Completed with `scripts/run_eeg_alignment.sh`.
4. Remove SLURM-specific launch complexity from the supported README and local path. Keep existing sweep scripts available until their dependencies and usefulness are understood; do not delete shared data/container utilities or unknown-purpose files without recording the decision.
5. Make EEG alignment evaluation produce stable CSV/YAML artifacts and correct effective-config serialization while keeping TensorBoard training-only. Completed with `test_metrics.csv` and `evaluation_config.yaml`.
6. Add the minimum tests/checks for launcher input validation, checkpoint/run discovery, evaluation serialization, and metric artifact contents.
7. Execute the local EEG alignment smoke experiment with the repository environment and available dummy data where possible. If the full Things-EEG2 run is too large or prerequisites are missing, run the smallest faithful smoke configuration and record the exact blocker for a real-data run. Completed on 2026-08-30 with the dummy dataset and a full 200-epoch Things-EEG2 subject-8 run.
8. Update README plus the missing `docs/project.md` and `docs/architecture.md` only with verified EEG alignment instructions and the forward-looking migration status. Update `.log`; retain `.plan` until this milestone is approved complete.
9. After EEG alignment is working, continue the remaining tracks incrementally rather than front-loading a large migration.
10. Controlled metric-reproduction iteration: run the canonical local launcher with the historical NICE/InfoNCE recipe as explicit overrides (`model.align_loss_type=infonce`, `model.encoder_lr=8e-3`, `model.encoder_min_lr=1e-4`, `model.max_epochs=100`). Compare the resulting CSV against the historical ~0.65 top-1 result before changing metric naming or evaluator direction.
11. Follow-up controlled recipe: the first InfoNCE comparison still retained the current `10 x MSE + 0.1 x InfoNCE` objective and EEG augmentation, so run pure InfoNCE with augmentation disabled (`model.align_mse_loss_factor=0`, `model.align_loss_factor=1`, `model.aug_eeg=false`) before concluding that the refactored implementation cannot reproduce the historical score.
12. Align the maintained EEG alignment contract with the requested experiment: keep the NICE encoder output directly in the 768-dimensional aligned SynCLR space with no projector, disable EEG augmentation in `train_eeg_align.yaml`, compute symmetric InfoNCE in both directions, and preserve both evaluator retrieval metrics (`brain_acc` and `image_acc`). Add focused tests for the loss directionality and metric naming, then run the full local experiment.
13. Clarification: alignment must use no MSE loss. Set the dedicated alignment config to pure symmetric InfoNCE (`align_mse_loss_factor=0`, `align_loss_factor=1`) and omit the zero-weight MSE term from the alignment loss dictionary, while preserving MSE support for other tracks/configurations.
14. Run an ATMS comparison with the same corrected alignment contract by overriding `eeg_encoder@model.eeg_encoder: atms`; verify the effective config and compare `brain_acc`, `image_acc`, and `sim` against the NICE run.
15. Select alignment checkpoints by the per-validation EEG-to-image top-1 metric (`trainer.checkpoint_monitor=val/align/top1`, mode `max`) and align the evaluator default checkpoint metric with the trainer filename. Keep both full-test `brain_acc` and `image_acc` outputs unchanged.
16. Sweep design/audit: do not use the stale W&B `scripts/tuning/sweep.py`, legacy NICE parameter files, or JSON-only aggregator as the primary alignment sweep. Define a local-first sweep manifest that fixes the alignment contract (no projector, no augmentation, symmetric InfoNCE, no MSE, aligned SynCLR, accuracy-based checkpointing), enumerates explicit encoder/seed/hyperparameter combinations, launches each run through the canonical train/evaluate path, and aggregates the CSV outputs with both `brain_acc` and `image_acc` plus effective hparams. Add a minimal SLURM array wrapper only after local sweep verification.
17. Implement the alignment sweep using the existing parameter-parser mechanism: update `scripts/params/sweep_align.json` to 2 image targets x 2 EEG encoders x 3 seeds (12 combinations), provide `scripts/evaluation/run_experiment_sweep.sh` as the local all-tasks launcher/aggregator, rename the SLURM pipeline to `run_experiment_sweep_slurm.sh`, and update aggregation to consume `test_metrics.csv` with alignment hparams.
   Completed: parser reports 12 combinations; local and SLURM launchers, CSV aggregation, README commands, and shell syntax checks are verified. The full 12-run sweep remains ready to launch but has not been started.
18. Reduce memory pressure for the alignment sweep by setting train, validation, and test batch sizes to 128 in `train_eeg_align.yaml`; verify the composed config before resuming or launching sweep jobs.
19. Limit alignment DataLoader parallelism to 4 workers (`dataset.num_workers: 4`) as a memory/speed compromise; verify the effective configuration before restarting the sweep.
20. Bound the process-wide tensor-cache LRU with `BRAIN_IMAGE_TENSORCACHE_MAXSIZE` (default 1024), document the setting, and verify invalid/valid environment overrides before restarting the sweep.
21. Completed the sequential 12-run alignment sweep with batch sizes 128, four workers, cache maxsize 1024, and CSV aggregation. Compare grouped NICE/ATMS and aligned/unaligned results in the handover report.

Feature list:
- Canonical EEG alignment single-experiment config and local train-then-evaluate shell workflow.
- Local-first launcher built by reusing the existing train-then-evaluate experiment behavior.
- Training TensorBoard output remains owned by training.
- Stable EEG alignment evaluation CSV/config/image artifacts.
- Explicit maintained/deprecated workflow table and removal inventory.
- README and architecture/project handover documentation.
- Automated checks for the new contracts.

Function/interface contracts:
- Proposed first launcher: a local wrapper around the existing `scripts/evaluation/run_experiment.sh` behavior, with the exact interface finalized after the script audit.
- SLURM wrapper is deferred until the local contract is verified.
- Proposed config: track, training config/script, evaluation script, output root, checkpoint selection/metric, evaluation metrics, and optional Hydra overrides.
- Evaluation input: run directory or explicit checkpoint.
- Evaluation output for EEG alignment: stable CSV rows, `evaluation_config.yaml`, selected-checkpoint metadata in the run structure, and reconstructed images where applicable. Evaluation does not write TensorBoard events or JSON metrics.
- Existing metric names remain unchanged unless a compatibility issue is found and approved.

Edge cases:
- Missing data, tensor caches, pretrained weights, or `.env` credentials.
- No checkpoint, malformed checkpoint filename, multiple `version_*` directories, or explicit checkpoint override.
- Evaluation metrics requiring optional models or GPU memory.
- Training configs that disable reconstruction while post-hoc evaluation forces it.
- Empty/partial experiment output after interruption.
- Local versus SLURM paths and shell argument quoting.
- Tracks that are preserved but temporarily blocked by missing external dependencies.

Side effects:
- New experiment/evaluation artifacts under the configured output root.
- No experiment launcher is deleted in the first milestone. Any later removal requires an explicit usage/dependency check and a `.log` entry.
- Evaluation may load pretrained metric networks and can be substantially more expensive than training validation.
- Existing sweep behavior may be removed from the handover interface after dependency checks.

Tests to add:
- Unit tests for canonical config parsing and required-field validation.
- Unit tests for checkpoint selection and deterministic output paths.
- Unit checks that EEG alignment evaluation metrics/config are valid CSV/YAML with expected columns and values.
- Shell-level tests for usage, missing-file, and dry-run failures.
- Dummy end-to-end smoke test where current dummy data/model dependencies permit it; otherwise record the exact prerequisite and defer it.

Possible refactors or suggestions:
- Centralize evaluation artifact writing instead of duplicating it in `test_eeg.py` and `test_comm.py`.
- Prefer one canonical launcher over the current generic five-positional-argument interface.
- Keep parameter sweeps out of the primary handover path; migrate or remove them after checking track dependencies.

TODOs:
- Define the EEG alignment CSV columns first; extend the schema per track later.
- Simplify the evaluator CLI's reconstruction metric options for alignment-only runs in a later cleanup.
- Decide whether to preserve, simplify, or archive each sweep/SLURM script after the local launcher is verified.
- Inventory broader track readiness after the first milestone.

Notes / risks / suggestions:
- The checkout has no `docs/project.md` or `docs/architecture.md`, despite AGENTS.md requiring them; create them during documentation work.
- README and sweep interfaces now use `scripts/params`, `run_experiment_sweep.sh`, and `run_experiment_sweep_slurm.sh`; retain the task-local helper as an implementation detail.
- `test_eeg.py` and `test_comm.py` currently write a JSON-encoded string instead of a JSON object for their config artifact; verify consumers before changing the schema.
- The existing training path already creates TensorBoard logs through Lightning. EEG evaluation writes separate CSV/YAML artifacts and must not be presented as a TensorBoard workflow.
- “Maintained” means each track has a documented config, launcher path, evaluator, artifact contract, and smoke/regression verification path; it does not imply every track is immediately runnable on every machine.
- Metrics must be documented as measured values from the current checkout, not inferred from notebook history.
- The sweep report notebook ranks runs by `image_acc` (EEG-to-image top-1), retains the reverse-direction `brain_acc`, and labels all per-run and top-three visualizations with the model, target, and seed.
- The first full current-config run reached `brain_acc=0.535` and `image_acc=0.470`; it used CLIP loss, so it is not a reproduction of the historical InfoNCE result.
- The first InfoNCE comparison reached `brain_acc=0.105` and `image_acc=0.430` at the selected epoch-9 checkpoint; it was not yet apples-to-apples because current MSE and augmentation remained enabled.
- User clarified the intended maintained alignment protocol: no projector, no augmentation, symmetric InfoNCE, and final metric named/reported as EEG-to-image top-1 accuracy.
- User further clarified that the alignment objective must contain no MSE loss; the current full run must be discarded/restarted after this change.

README updates:
- Replace the top-level workflow with a concise EEG alignment quick start.
- Add prerequisites, canonical local config/launcher examples, artifact layout, training TensorBoard commands, evaluation CSV inspection, and exact metric recreation commands.
- Remove the multi-step SLURM pipeline from the primary README; document existing sweep launchers separately only if they remain supported after the audit.
- Correct command paths and argument signatures.
- Link the executed `notebooks/eeg_alignment_sweep_report.ipynb` and describe its per-seed table, across-seed summary, and top-three match visualization.

22. Add a clean executed report notebook for the completed 12-run alignment sweep. Load `experiments/eeg_alignment_sweep/aggregated_metrics.csv`, validate the expected 12-run schema, present per-run metrics and grouped seed summaries, identify the best runs by `image_acc`, and visualize target/chosen pairs for the top three runs with encoder, target, and seed labels. Validate notebook execution against the existing artifacts.
   Completed: `notebooks/eeg_alignment_sweep_report.ipynb` executes successfully and README links to it.

[2026-08-31 00:00] Prior/reconstruction handover path audit

Request summary:
- Move from EEG alignment to the prior/reconstruction track.
- Trace the current code path and understand which existing configuration values affect alignment, prior training, validation, sampling, image reconstruction, and reported metrics.
- After review, add a separate prior/reconstruction training entrypoint with sensible defaults; do not implement it before this plan is reviewed.

Implementation steps:
- Audit `train_eeg_prior.yaml` and `train_eeg_alignprior.yaml` against `train_eeg.py`, `EEGAlignmentModel`, `SimpleDiffusionPrior`, `test_eeg.py`, and `reconstruction.py`.
- Decide and document the canonical scope: prior-only (`do_align=false`) versus joint alignment+prior (`do_align=true`), with prior-only as the likely first reproducible baseline because it isolates reconstruction learning.
- Add a dedicated training entrypoint/config only after approval, reusing the existing model/trainer and train-then-evaluate conventions.
- Define conservative local defaults for batch size, worker count, cache bound, epoch/evaluation cadence, checkpoint metric, prior target encoder/stats, augmentation, and reconstruction validation cost.
- Add a local shell launcher that trains sequentially and invokes evaluation separately; keep environment activation outside the launcher.
- Verify composed configuration, one-epoch dummy execution, checkpoint selection, CSV metrics, reconstruction image artifacts, and TensorBoard training artifacts.
- Update README, project/architecture docs if present or create the currently missing docs, `.plan`, and `.log` after implementation.

Feature list:
- Maintained prior/reconstruction training command.
- Explicit separation between prior training loss/validation and expensive image reconstruction evaluation.
- Stable CSV/YAML evaluation artifacts plus reconstruction image pairs.
- Documented relationship between EEG latent, prior image latent, diffusion prior, and reconstruction pipeline.

Function/interface contracts:
- Proposed training CLI: dedicated script with Hydra `train_eeg_prior` config and normal Hydra overrides.
- Proposed local CLI: `scripts/run_eeg_prior.sh`, train first and then call the separate evaluator on the produced run.
- Training output: checkpoint, Lightning TensorBoard events, and serialized hyperparameters under one run directory.
- Evaluation output: `test_metrics.csv`, `evaluation_config.yaml`, and selected reconstruction/ground-truth image artifacts; no evaluation TensorBoard stream.
- Primary checkpoint metric: validation prior cosine (`eval/val/prior/pred_cos`) in max mode. It is emitted by full validation and is available before expensive image reconstruction metrics.

Edge cases:
- `train_eeg_prior.yaml` currently overrides augmentation to EEG, disables alignment, enables the prior, and sets `skip_reconstruction=true`; this means training validation does not generate image reconstructions, while `test_eeg.py` forcibly enables them.
- The prior target is `prior_img_latent`, while its stats are loaded for `prior_img_encoder`; changing `prior_img_encoder` requires matching cached embeddings and stats.
- `prior.norm_scheme=z_scale` requires compatible embedding statistics.
- Prior conditioning dimension is derived from the EEG encoder output, while prior input dimension is derived from the selected image encoder.
- `prior_delay_epochs` controls whether prior loss runs during training, but prior validation runs regardless of the delay.
- Reconstruction metrics are expensive and may require optional pretrained networks/GPU memory; avoid running them every epoch by default.
- Full reconstruction sampling is stochastic and must use the configured seed for comparable evaluation.
- Existing generic `test_eeg.py` defaults to alignment checkpoint selection; a prior-specific evaluator override or script is required.

Tests to add:
- Config composition assertions for prior-only mode, selected encoder/stat keys, no accidental alignment loss, and checkpoint monitor.
- Minimal model/path test that verifies prior dimensions and required stats are derived from the selected image encoder.
- Dummy one-epoch train/evaluate smoke test covering checkpoint, CSV/YAML, TensorBoard, and reconstruction outputs.
- Regression check that evaluator selects the intended prior/reconstruction checkpoint metric.

TODOs:
- Choose the checkpoint metric after auditing trainer logging and reconstruction metric availability.
- Decide whether the first maintained run should use NICE or ATMS and which `prior_img_encoder` is the canonical target.
- Decide whether EEG augmentation remains enabled for the prior-only baseline.
- Determine whether a prior-specific evaluator wrapper is needed or whether `test_eeg.py` can be safely parameterized.

Notes / risks / suggestions:
- Existing `train_eeg_prior.yaml` is the closest starting point; `train_eeg_alignprior.yaml` is a joint-training variant and should remain a comparison rather than silently becoming the default.
- The current model always creates `align_loss`, but `do_align=false` prevents it from contributing to the loss; this should be verified rather than treated as a separate architectural path.
- The current prior objective is weighted cosine loss plus Huber loss plus a CLIP-style prior alignment loss; the commented MSE is not active.
- The current full-test metric set includes image metrics such as pixcorr, SSIM, AlexNet, Inception, CLIP, EfficientNet, and SwAV. The first smoke run should use a small explicit subset if optional models make the full set impractical.

README updates:
- Add a prior/reconstruction quick start only after the dedicated path is implemented and verified.
- Document the prior target encoder, checkpoint metric, training TensorBoard location, evaluation CSV, and reconstruction image outputs.

Decisions for the first implementation iteration:
- Use prior-only training (`do_align=false`, `do_recon=true`).
- Set train/validation/test batch sizes to 32 and keep the established four workers and bounded tensor cache defaults.
- Initialize the EEG encoder from the completed best alignment checkpoint: `experiments/eeg_alignment_sweep/20260830_231325_task4/train_eeg-20260830_231357/version_0/checkpoints/epoch_0089-val_align_top1_0.7650.ckpt` (ATMS, aligned SynCLR, seed 42).
- Keep the prior target as `clip_vith14` for the first run, because this is the existing prior-only default and its `prior_img_latent` statistics are available through the matching encoder name.
- Preserve `skip_reconstruction=true` during training to avoid expensive image sampling each validation cycle; evaluate reconstructions separately.
- Implement the first iteration as the dedicated prior training entrypoint/config and checkpoint initialization wiring. Defer launcher/evaluator specialization until the composed configuration and checkpoint load are verified.

Iteration 1 status:
- Completed `src/brain_image/configs/train_eeg_prior.yaml` updates: prior-only mode, batch sizes 32, four workers, and the finished ATMS alignment checkpoint as `model.eeg_encoder_path`.
- Added `scripts/training/train_eeg_prior.py` with a dedicated Hydra entrypoint and full-checkpoint resume support through the existing trainer contract.

Iteration 2 status:
- Set prior checkpoint monitoring to `eval/val/prior/pred_cos` with max mode and five-epoch full validation cadence.
- Added `scripts/run_eeg_prior.sh` to train and then evaluate sequentially, explicitly selecting the best prior cosine checkpoint.
- Updated README with the prior-only local workflow and artifact/checkpoint behavior.
- Verified shell syntax, Hydra composition, and extraction of 45 `eeg_encoder.*` tensors from the configured alignment checkpoint. No full or dummy training run has been launched yet.

Smoke-test finding:
- A one-epoch prior smoke run loaded the configured alignment checkpoint and completed, but inherited `skip_eval_first_epoch=true` prevented the prior cosine metric from being emitted and therefore prevented a monitored top-k checkpoint. Set `skip_eval_first_epoch=false` for the prior config so short runs and checkpoint selection are observable.
- The first launcher smoke showed the exact logged key is `eval/val/prior/pred_cos` because full-validation metrics receive an `eval/` prefix; the trainer monitor and evaluator argument now use this key.
- Corrected launcher smoke passed end to end on a 1% subset: selected `epoch_0000-eval_val_prior_pred_cos_0.6243.ckpt`, wrote `test_metrics.csv` and `evaluation_config.yaml`, and completed separate evaluation. The reduced test subset did not include highlighted reconstruction indices, so no reconstruction image pairs were expected.

Full-run status:
- Launched the full prior-only experiment sequentially in tmux session `eeg_prior_full` with `BRAIN_IMAGE_TENSORCACHE_MAXSIZE=1024`.
- Output root: `experiments/eeg_prior/20260831_134809`.
- Startup confirms the intended prior target (`clip_vith14`), max checkpoint monitor (`eval/val/prior/pred_cos`), batch size 32, four workers, and the finished alignment EEG checkpoint initialization.
- Completed training and separate reconstruction evaluation. Best checkpoint was epoch 289 with validation prior cosine 0.6340; test prior cosine was 0.6340 and all nine highlighted reconstruction pairs were saved.

23. Document the prior-only baseline separately from the EEG-Guided Diffusion paper comparison. Add a notebook that loads the measured CSV, shows the exact protocol and metrics, and visualizes the nine reconstruction/ground-truth pairs. State clearly that the current implementation omits paper-specific reconstruction components, so the paper's reported scores are not direct acceptance targets.
   Completed: README updated with the baseline interpretation and measured values; `notebooks/eeg_prior_reconstruction_report.ipynb` added and executed against the full-run artifacts.

24. Add a separate historical notebook for the second-prior experiment. Reference commit `44f8854` where the second latent/prior path was introduced and commit `2cca62a` where it was removed. Summarize the concatenated second-latent design, its additional losses/metrics, the recorded `prior_second` results, and why it is not maintained.
   Completed: `notebooks/second_prior_history.ipynb` added and executed; README links to it and labels the approach historical/non-working.

[2026-09-01 00:00] Identify the next historical experiment track

Request summary:
- Reconstruct the actual order of work from the current README and git history.
- Determine whether CoMM was the next track after the maintained EEG alignment/prior work, rather than assuming the next implementation step from the handover sequence.
- Do not modify production code until the evidence and proposed next step are reviewed.

Feature list:
- Evidence-backed track chronology.
- Current-checkout inventory of the CoMM training config, training script, evaluator, model code, and existing report notebook.
- Explicit distinction between historical order and maintenance priority.

Implementation steps:
1. Read README track descriptions and current workflow sections.
2. Inspect dated git history and commits touching CoMM, text alignment, and low-level code.
3. Inspect current CoMM config, model/training/evaluation entrypoints, and artifacts/notebooks.
4. Report whether CoMM was historically next and identify the smallest verification task before any cleanup or rewrite.
5. After approval, add only the agreed CoMM verification/maintenance changes in a small iteration. Completed in the first implementation iteration.

Function/interface contracts:
- No production interface changes in this audit iteration.
- Candidate maintained path, subject to review: a simple CoMM train-then-evaluate launcher with CSV metrics and a documented checkpoint-selection rule, matching the alignment/prior handover conventions.

Edge cases:
- README ordering may differ from chronological commit order.
- Current CoMM scripts may still use JSON metrics, in-process testing, or stale config names.
- Existing notebooks are historical evidence, not automatically valid acceptance tests.
- `docs/project.md` and `docs/architecture.md` are absent despite AGENTS.md requiring them; do not silently invent architecture changes during this audit.

Tests to add:
- Added `tests/test_comm_evaluation.py` for scalar metric CSV serialization.
- Verified config composition and a minimal train/evaluate artifact smoke test on the dummy dataset.

TODOs:
- Decide whether to run a full Things-EEG2 CoMM experiment or first document the existing historical results in a clean report.
- Audit the separate low-level and text-alignment paths after CoMM.

Notes / risks / suggestions:
- History currently shows CoMM implementation/evaluation commits in February 2026 and text alignment implementation in March 2026, after the main refactor.
- The README describes CoMM before text alignment and low-level work, but chronology and current readiness must be reported separately.

README updates:
- Added the CoMM local launcher, batch-size, checkpoint-selection, artifact, TensorBoard, and historical-status documentation.

Implementation iteration 1 status:
- Updated `train_comm.yaml` to use batch size 32 for all splits, four workers, and max-mode checkpointing on `val/acc_eeg_to_img`.
- Changed `train_comm.py` to train only; test execution is now separate.
- Updated `test_comm.py` to write `test_metrics.csv` and `evaluation_config.yaml`, with max-mode EEG-to-image checkpoint selection by default.
- Added `scripts/run_comm.sh` for sequential local train-then-evaluate execution.
- Added and passed `tests/test_comm_evaluation.py`.
- Passed Python compilation, shell syntax, Hydra composition, and dummy end-to-end smoke test. Smoke output: `experiments/comm_smoke/20260901_004749/train_comm-20260901_004757`.

Implementation iteration 2 status:
- Started the full Things-EEG2 CoMM train-then-evaluate run in tmux session `comm_full`.
- Run root: `experiments/comm/20260901_082138`.
- Environment is activated inside tmux; `BRAIN_IMAGE_TENSORCACHE_MAXSIZE=1024` is set.
- Stopped before completion after confirming that the current path consumes `align_img_latent` directly; this run is invalid for the intended post-prior CoMM protocol and must not be reported.

Implementation iteration 3 plan (pending review): post-prior CoMM protocol

Request summary:
- CoMM must fuse the EEG latent with the image latent generated by the trained diffusion prior, not the ground-truth/precomputed image latent.
- Do not launch another run until the prior checkpoint, conditioning path, generated latent dimensionality, and evaluation protocol are wired and smoke-tested.

Feature list:
- Explicit prior checkpoint/config inputs for CoMM.
- Per-batch frozen diffusion-prior generation from the EEG latent.
- CoMM fusion using `[EEG latent, generated prior image latent]`.
- Separate evaluation that reports retrieval metrics and records the exact prior-generation settings.

Implementation steps:
1. Trace the historical `notebooks/comm_prior.ipynb` setup and current prior checkpoint/model loading APIs.
2. Add a config contract for the finished prior checkpoint and generation parameters; keep batch size 32 and avoid loading the ground-truth image latent as the CoMM image modality.
3. Add the smallest testable model helper that maps EEG to a generated prior latent and feeds that latent into CoMM; freeze the prior during CoMM training unless the historical protocol explicitly says otherwise.
4. Update the local launcher/evaluator to use the post-prior path and write CSV/YAML artifacts.
5. Run a dummy or one-batch smoke test that proves the CoMM image input is generated by the prior, then run the full experiment sequentially in tmux.

Function/interface contracts:
- CoMM training requires a prior checkpoint path and prior generation settings in the effective config.
- Evaluation artifacts must include those settings and retrieval metrics in `test_metrics.csv`/`evaluation_config.yaml`.
- The invalid run at `experiments/comm/20260901_082138` is not an acceptance result.

Edge cases:
- A prior-generated latent may have a different encoder/normalization contract than `align_img_latent`; validate dimensions and normalization explicitly.
- Prior generation is stochastic and may be expensive; generation seed/steps/guidance must be fixed for comparable runs.
- The prior must not receive ground-truth image latents as conditioning or targets during CoMM evaluation.
- The current CoMM data module still loads `align_img_latent`; remove that dependency from the post-prior path or isolate it to optional diagnostic metrics.

Tests to add:
- Config/checkpoint existence and dimensionality test.
- Unit or smoke assertion that the CoMM image branch receives prior output rather than `batch["align_img_latent"]`.
- One-batch dummy train/evaluate artifact test after the prior path is wired.

TODOs:
- Confirm the canonical completed prior checkpoint and whether the prior EEG encoder should be shared with or separate from the trainable CoMM EEG encoder.
- Decide whether the generated prior latent is used directly or normalized exactly as in `comm_prior.ipynb`.

Notes / risks / suggestions:
- The current full run was stopped at approximately epoch 3 and is retained only as an invalid-protocol artifact.
- Historical notebook code generated `clip_pred = prior.generate(conditioning=eeg_latent, ...)` and fused `eeg_latent` with `clip_pred`; this is the protocol to reproduce.

README updates:
- Correct the CoMM description and launcher documentation after the post-prior implementation is verified; do not present the current precomputed-latent path as the intended experiment.

Implementation iteration 3 status:
- Added frozen prior loading from the completed prior checkpoint, including checkpoint-derived prior configuration and matching Things-EEG2 statistics.
- CoMM can generate post-prior image latents from the aligned EEG encoder in chunks and uses the generated 1024-dimensional `clip_vith14` latent as its image modality.
- The historical-faithful default trains from cached target `clip_vith14` latents and switches to generated prior latents for evaluation.
- Added deterministic generation settings to evaluation YAML metadata.
- Added and passed generation-helper tests; corrected model smoke completed at `experiments/comm_postprior_smoke/20260901_084015`.
- The full corrected run has not yet been launched. Historical `comm_prior.ipynb` trains CoMM from cached image latents and applies the generated prior latent during post-training evaluation.
- The full corrected run is now launched sequentially in tmux session `comm_full_correct` at `experiments/comm/20260901_084821`.

Follow-up decision required:
- Preferred historical-faithful option: train CoMM on cached `clip_vith14` latents, then evaluate the trained CoMM with generated post-prior `clip_vith14` latents and record both protocols explicitly. Approved and implemented.
- Alternative: precompute and cache generated prior latents before CoMM training, which is a new data-generation step and must be benchmarked separately.

[2026-09-01 00:00] Correct CoMM ground-truth retrieval metrics

Request summary:
- Measure the actual research question: whether the fused EEG plus diffusion-prior(EEG) representation retrieves the ground-truth image better than EEG alone.
- Preserve existing generated-branch diagnostics, but add explicit retrieval metrics against the cached target `clip_vith14` latent.

Feature list:
- Ground-truth target image latent retained during evaluation.
- EEG-to-target, generated-prior-to-target, and fused-prototype-to-target top-1 metrics.
- CSV/YAML artifact documentation for the new metric names and evaluation protocol.

Implementation steps:
1. Extend `CommAlignmentModel.get_retrieval_accuracies` to accept the target latent separately from the generated image latent.
2. Keep the current generated-branch metrics unchanged and add target-reference metrics with unambiguous names.
3. Update `test_comm.py`, tests, and README metric descriptions.
4. Re-evaluate the finished CoMM checkpoint without retraining; compare fused prototype-to-target against EEG-to-target on the same test examples.

Function/interface contracts:
- `prepare_batch` continues to use cached targets for training and generated prior latents for test-time CoMM input.
- Test metrics must distinguish `generated_img` from `target_img`; no metric may call a generated latent “ground truth.”

Edge cases:
- Target and generated latents must have matching dimensionality and normalization (`clip_vith14`, 1024 dimensions).
- Existing metric names must remain available for backward compatibility.
- Checkpoint selection remains based on validation metrics, while final comparison uses target-reference test metrics.

Tests to add:
- Unit test for target-reference retrieval metric keys and directionality.
- Evaluation smoke check that the new target-reference rows are written to CSV.

TODOs:
- Re-evaluate the completed run after implementation and record the three-way comparison in the CoMM report.

Notes / risks / suggestions:
- The current `acc_eeg_to_proto=0.61` is a generated-branch diagnostic, not the primary target-reference result.
- This is an evaluator-only correction; no CoMM retraining is required.

README updates:
- Document target-reference metrics as the primary comparison for the CoMM fusion hypothesis.

Implementation iteration 4 status:
- Added explicit target-reference top-1 retrieval metrics while preserving the existing generated-branch diagnostics.
- Re-evaluated the selected checkpoint `epoch_0024-val_acc_eeg_to_img_0.5400.ckpt` from the completed historical-faithful run.
- Corrected test CSV results: EEG to target image `0.25`, generated prior image to target image `0.095`, and fused prototype to target image `0.19`; reverse-direction values are `0.235`, `0.05`, and `0.145` respectively.
- Added a unit test covering metric keys and directionality; all three CoMM evaluator tests pass.
- The fused target-reference score is below the EEG-only baseline on this run, so the fusion hypothesis is not supported by this checkpoint.

Next step:
- Keep the evaluator correction and protocol documentation; any improvement attempt should be planned as a separate training experiment rather than changing metric definitions again.

[2026-09-01 15:45] CoMM experiment report notebook

Request summary:
- Create a maintained notebook report for the CoMM experiment, consistent with the existing alignment and prior reports.
- Explain why the experiment was performed: test whether a diffusion-prior image latent adds useful information to an EEG representation for image retrieval.
- Report the exact pipeline, evaluation protocol, metrics, checkpoint, and measured result.

Implementation steps:
1. Inspect the existing CoMM report notebooks and current CSV/YAML artifacts for reusable style and verified values.
2. Create `notebooks/comm_report.ipynb` with markdown explaining the hypothesis and pipeline, plus executable cells loading the final `test_metrics.csv` and `evaluation_config.yaml`.
3. Add tables comparing EEG-only, generated-prior-only, and fused-prototype retrieval in both directions.
4. Add a compact visualization of the primary top-1 results and a conclusion grounded in the measured numbers.
5. Execute the notebook if the local environment supports it and verify that all displayed metrics are read from the saved artifacts.
6. Link the notebook from README and update `.plan` and `.log`.

Feature list:
- Reproducible CoMM experiment report notebook.
- Explicit distinction between ground-truth target-reference metrics and legacy generated-branch diagnostics.
- Scientific motivation, pipeline diagram/table, configuration provenance, results, and conclusion.

Function/interface contracts:
- The notebook reads, rather than re-derives, final values from `test_metrics.csv`.
- The primary comparison uses `acc_eeg_to_target_img`, `acc_generated_img_to_target_img`, and `acc_proto_to_target_img`.
- The notebook records the selected checkpoint and prior-generation settings from `evaluation_config.yaml`.

Edge cases:
- The invalid pre-correction CoMM run must not be reported.
- Missing artifacts or unavailable notebook execution dependencies must be recorded rather than silently substituted.
- Legacy generated-branch metrics must not be presented as ground-truth retrieval.

Tests to add:
- Notebook execution or equivalent cell-level validation.
- Assert that the expected CSV metric names and values are present in the report data.

TODOs:
- None beyond implementation and verification of this report.

Notes / risks / suggestions:
- The selected checkpoint was chosen using validation `acc_eeg_to_img`, while the final scientific comparison uses target-reference metrics; this distinction must be stated explicitly.
- Current measured primary results are EEG-only `0.25`, generated prior `0.095`, and fused prototype `0.19`.

README updates:
- Link the new CoMM report notebook from the CoMM section.

[2026-09-01 16:00] Verify and document Qwen text embeddings

Request summary:
- Audit and document the text-conditioning assets needed for the next Text Alignment experiment.
- Verify what Qwen does, which text encoder produces the actual alignment target, where captions/embeddings/statistics are stored, and how the training config consumes them.
- Resolve naming ambiguity so “Qwen embeddings” is not incorrectly used for the downstream T5 or CLIP text embeddings.

Verified initial facts:
- `scripts/data/generate_text_captions_local.py` uses `Qwen/Qwen2.5-VL-7B-Instruct` to generate captions from stimulus images.
- Captions are appended as JSONL records with `path` and `caption` fields under `data/things-eeg2/captions/local.jsonl` by default.
- `scripts/data/generate_text_embeddings.py` encodes those captions using configured downstream text encoders; the default list is `t5_base`, `clip_vitl14_text`, and `gemma_embedding_300m`.
- `train_eeg_align_text.yaml` currently selects `align_img_encoder: t5_base`; Qwen is therefore a caption generator, not the alignment embedding model.
- `generate_text_stats.yaml` computes normalization statistics for the selected cached text embeddings.

Implementation steps:
1. Trace caption generation, embedding generation, statistics, dataset lookup, and text-alignment consumption end to end, including dimensions and normalization.
2. Inspect available cached captions, text embeddings, and statistics and verify path/key/shape consistency without launching a large model job.
3. Add focused tests or a lightweight validation command for JSONL uniqueness, embedding-cache lookup, dimensions, and statistics availability.
4. Document the verified Qwen caption-generation and downstream text-embedding workflow in README and a short notebook/report under `notebooks/`.
5. Only after the assets are verified, decide whether to run the Text Alignment experiment and define its local launcher/config defaults.

Feature list:
- Explicit Qwen caption-generation contract.
- Explicit downstream text-encoder contract for Text Alignment.
- Reproducible artifact validation and documentation.

Function/interface contracts:
- Caption records must contain a stable image path and non-empty caption.
- Each selected text encoder must have matching cached embeddings and per-split statistics.
- `train_eeg_align_text.yaml` must reference an encoder whose cache and statistics exist for the configured dataset/splits.

Edge cases:
- Qwen caption generation may be unavailable locally due to model weights or GPU memory.
- Existing JSONL files may be partial, duplicated, or generated with a different prompt/model.
- The config’s `sub=1` scope and split coverage must match the available caches.
- Text encoder output dimensions and normalization statistics must agree with the EEG alignment target.

Tests to add:
- Lightweight artifact validation for caption schema, cache coverage, dimensions, and statistics.
- Config-level assertion that the selected `align_img_encoder` is supported and available.

TODOs:
- Confirm whether the existing `local.jsonl` was generated with the current Qwen model/prompt or must be regenerated.
- Decide whether the maintained text-alignment default should remain `t5_base` or use another verified encoder.

Notes / risks / suggestions:
- Call the artifacts “Qwen-generated captions” and “T5/CLIP/Gemma text embeddings” separately in all documentation.
- Do not run Qwen generation or Text Alignment until the artifact audit identifies a complete, internally consistent input set.

README updates:
- Clarify the two-stage caption/embedding workflow and identify `t5_base` as the current Text Alignment target.
- Document validation commands and any verified artifact paths.

Implementation iteration status:
- Audited the live artifacts: 16,740 unique non-empty captions, all referenced images present, and complete train/test caches for all three configured text encoders.
- Verified 768-dimensional float32 statistics for `t5_base`, `clip_vitl14_text`, and `gemma_embedding_300m`.
- Added `scripts/data/validate_text_artifacts.py` and focused parser tests.
- Added `notebooks/qwen_text_embedding_report.ipynb` and updated README terminology/documentation.
- Corrected the validator to match the repository cache convention (`image.jpg` maps to `image.pt`).
- Full validation passes: 16,740 captions, 16,540 train images, 200 test images, complete caches/statistics, and sample tensor dimensions verified.
- Focused tests pass (2/2), and the report notebook executes successfully.

Conclusion:
- The existing artifacts are internally consistent for the current `t5_base` Text Alignment configuration.
- Qwen generation provenance is incomplete in the JSONL artifact because model/prompt metadata is not stored per record; the current generation config remains the documented source of truth.
- No regeneration is required for the artifact-validation milestone. The next separate task is to prepare/run Text Alignment using the verified `t5_base` target.

[2026-09-01 20:55] Regenerate Qwen caption and text artifacts

Request summary:
- Regenerate the caption artifacts with the configured Qwen model so the current handover has a fresh, explicit generation run.
- Rebuild downstream text embeddings and normalization statistics from the regenerated captions.
- Preserve the existing artifacts as a baseline; do not overwrite them.

Implementation steps:
1. Use a versioned caption path, such as `data/things-eeg2/captions/qwen2.5-vl-7b-instruct.jsonl`, with the current Qwen model and prompt from `generate_text_captions_local.yaml`.
2. Verify the regenerated JSONL has complete train/test coverage, unique paths, non-empty captions, and recorded generation metadata alongside the artifact documentation.
3. Generate `t5_base` embeddings from the new caption file into a separate cache directory.
4. Generate matching train/test `t5_base` statistics into a separate statistics directory.
5. Run the artifact validator against the regenerated caption/cache/statistics set, including tensor dimensions.
6. Update the Qwen report and README with the versioned paths, model/prompt settings, counts, and validation result.

Feature list:
- Reproducible Qwen caption regeneration.
- Isolated downstream embedding/statistics artifacts.
- Provenance-aware handover documentation.

Function/interface contracts:
- Existing `local.jsonl`, shared `tensorcache`, and shared `statistics` remain unchanged.
- The regenerated caption path is passed explicitly to embedding generation.
- The regenerated cache/statistics paths are passed explicitly to all validation and later Text Alignment commands.

Edge cases:
- Qwen weights or required packages may be unavailable, or GPU memory may be insufficient.
- Caption generation is append/resume-oriented; a pre-existing versioned target must not be mixed with a different model/prompt.
- Qwen can produce malformed or empty output; validation must fail loudly.
- The generated text embedding cache must use the exact same image-path key convention as the training dataset.

Tests to add:
- Extend artifact validation to check optional provenance metadata and split consistency.
- Run the validator with tensor-shape checks on the regenerated artifacts.

TODOs:
- Use `qwen2.5-vl-7b-instruct.jsonl` plus a matching YAML sidecar for the regenerated caption artifact.

Notes / risks / suggestions:
- Regeneration is expected to be expensive for 16,740 images; run it in a user-visible terminal/tmux session, not a hidden background process.
- Rebuilding only `t5_base` is sufficient for the next Text Alignment experiment; optional CLIP/Gemma regeneration can remain deferred.

README updates:
- Document the versioned regenerated artifact paths and exact commands.
- Distinguish the regenerated, provenance-aware artifacts from the legacy `local.jsonl` baseline.

Implementation iteration status:
- Added caption-generation provenance sidecar support. The generator now records model, prompt, decoding limit, batch size, dtype, device, splits, and caption path before generation.
- Existing caption generation behavior and legacy artifacts are unchanged.
- GPU-dependent generation is paused by user request before launch. No Qwen model was loaded and no versioned artifacts were created.
- Regeneration is now running in tmux session `qwen_caption_regen` with the activated `.venv`, using `Qwen/Qwen2.5-VL-7B-Instruct`, the configured 512-token prompt, and the versioned output paths.
- At the latest check, model files were still being fetched; no regenerated caption records had been written yet.
- The initial launch was cancelled before model loading completed. The exact process was stopped, and the configured Qwen caption batch size was reduced from 32 to 1 for the next launch.
- Relaunched the versioned regeneration in tmux session `qwen_caption_regen`; effective Hydra configuration confirms `batch_size: 1`. The job is currently loading Qwen and has not written caption records yet.
- The batch-size-1 launch failed during model loading with CUDA OOM: another Python process occupied approximately 12.3 GiB of the 23.5 GiB GPU, leaving approximately 146 MiB free. No caption records were generated; the existing artifacts remain untouched.
- Added `device_map: auto` to the local Qwen configuration and passed it to model loading; input placement remains controlled separately by `device`.
- CPU-offloaded relaunch succeeded in loading all five Qwen checkpoint shards with `device_map=auto` and batch size 1. The process advanced to train-dataset preparation; caption generation has not started yet.
- The first offloaded run stopped before captioning because the script used the stale `embeddings_to_compute_stats` dataset keyword. Updated it to the current `load_embedding_stats` interface; no partial captions were created.
- The next run exposed that `EEGDataModule.create_dataset()` already injects `load_embedding_stats`, so the explicit empty override caused a duplicate keyword. Removed the redundant argument; no partial captions were created.
- Captioning is now active: 2/16,540 train images completed at roughly 7.6 seconds per image, implying approximately 35 hours for the train split. CPU offloading works but is too slow for an efficient full regeneration at batch size 1.
- Switched the approved regeneration target to `google/gemma-4-E2B-it`; added a model-specific Gemma multimodal loader/chat-template path and changed the default caption config accordingly. Existing Qwen partial artifacts remain preserved.
- Gemma smoke startup exposed an incompatibility between Transformers 5.16 development code and the repository's older `peft`/`diffusers` imports. Simplified caption generation to enumerate image paths directly, avoiding unnecessary EEG/diffusion imports and preloading.
- Removed the remaining dataset-union type annotation import, which transitively loaded the diffusion encoder stack. The caption script now imports only the image-path utility and treats the dataset config as an opaque field container.
- Gemma model loading succeeded with offloading, but the smoke test exposed that the opaque Hydra dataset config is a dict. Updated image-path and limit-size access to use dictionary keys; no caption records were created.
- Added persistent Hugging Face cache exports to `/home/gasparyanartur/.bashrc`, pointing `HF_HOME`, `HF_HUB_CACHE`, and `TRANSFORMERS_CACHE` to `/storage/huggingface`; existing home-cache contents were not removed.

[2026-09-02 19:00] Switch caption regeneration to Gemma 4 E2B

Request summary:
- Replace the impractically slow Qwen 7B caption regeneration with Google's multimodal Gemma 4 E2B checkpoint.
- Preserve the versioned artifact workflow and provenance metadata.

Implementation steps:
1. Use checkpoint `google/gemma-4-E2B` and verify the local Transformers version exposes its multimodal loader.
2. Adapt the caption-generation loader and processor call to Gemma 4's `AutoModelForMultimodalLM` interface and chat template.
3. Run a one-image or five-image smoke test at batch size 1 before full generation.
4. If the smoke test succeeds, generate the full train/test caption set into a new Gemma-specific JSONL and YAML provenance sidecar; do not mix it with the partial Qwen output.
5. Rebuild only the `t5_base` embeddings and statistics from the Gemma captions after caption generation completes.
6. Validate and document model name, prompt, counts, cache/statistics paths, and the fact that the caption generator changed from Qwen to Gemma.

Feature list:
- Gemma 4 E2B image-caption generation.
- Separate provenance-aware Gemma caption artifacts.
- Rebuilt downstream `t5_base` artifacts.

Function/interface contracts:
- Caption generation remains image-plus-label to one text caption per image.
- Model placement uses `device_map=auto`; input placement follows the loaded model's device contract.
- Existing Qwen, legacy captions, embeddings, and statistics remain unchanged.

Edge cases:
- `google/gemma-4-E2B` requires a Transformers version with Gemma 4 support and may require model access acceptance/authentication.
- Gemma's chat-template and image-input format may differ from Qwen's processor API.
- The two partial Qwen records are not reused or mixed into Gemma output.
- Gemma output quality is a methodological change and must be recorded in the report.

Tests to add:
- Unit-test the model-agnostic caption message construction/response trimming where practical.
- Smoke-test Gemma loading and one-image generation before full execution.

TODOs:
- Confirm exact local Transformers compatibility and the model's output format.

Notes / risks / suggestions:
- The Qwen job was stopped at exactly two records; its partial file is retained for debugging but is not a valid dataset.
- Gemma 4 E2B is documented by Google as multimodal and supports `AutoModelForMultimodalLM` with automatic device mapping.

README updates:
- Update the regenerated-artifact documentation only after the Gemma smoke/full run is verified.

[2026-09-02 09:40] Enable CPU offloading for Qwen caption generation

Request summary:
- Allow Qwen caption regeneration to run while another process occupies GPU memory.
- Use automatic device mapping/offloading rather than requiring the full 7B model on CUDA.
- Keep caption batch size at 1 and preserve the versioned output/provenance paths.

Implementation steps:
1. Add an explicit `device_map` setting to the local caption-generation config, defaulting to `auto`.
2. Pass `device_map=auto` to Qwen model loading while keeping processor inputs on the configured execution device.
3. Run a startup/small-scope validation in the existing tmux session and inspect GPU/CPU placement before full generation.
4. If model loading succeeds, resume the versioned full caption generation and validate the resulting artifacts.

Feature list:
- CPU/GPU offloaded Qwen loading.
- Batch-size-1 memory-safe caption generation.

Function/interface contracts:
- `device` controls input placement; `device_map` controls model placement.
- Existing CUDA-only behavior can be restored with `device_map=cuda` if desired.
- Existing caption artifacts remain unchanged.

Edge cases:
- Automatic offloading may be very slow and requires sufficient host RAM and an Accelerate-compatible Transformers installation.
- If the model cannot fit across available CPU/GPU memory, use a smaller or quantized model instead.
- A failed startup must not leave partial caption records mixed with the versioned output.

Tests to add:
- Config/composition check that `device_map=auto` is accepted and passed to the loader boundary.
- Keep existing caption parser/artifact validation tests.

TODOs:
- Run the offloaded generation and validate its completion.

Notes / risks / suggestions:
- The current OOM occurred during model loading, so lowering batch size alone cannot solve it.

README updates:
- Document the optional `device_map=auto` CPU-offloading override after successful verification.

Implementation status:
- Created `notebooks/comm_report.ipynb` with artifact-backed protocol, checkpoint, primary metric table, visualization, and interpretation.
- Linked the report from README.
- Notebook validation completed successfully: the notebook parsed as nbformat 4 and all four code cells executed against the final CSV/YAML artifacts.
[2026-09-02 19:20] Relocate experiment logs and outputs to /storage

Request summary:
- Move the brain-image-implementation logs and experiments out of the nearly-full home filesystem.
- Store them at `/storage/logs/brain-image-implementation` and `/storage/experiments/brain-image-implementation`.
- Preserve the existing repository paths as symlinks.
- Apply the same storage convention to `~/dev/pose-graph-diff/external/diffusionregpose/logs` if that checkout/path exists.

Implementation steps:
1. Confirm no active training/evaluation process is writing the two source directories.
2. Verify destination paths do not already contain unrelated data and create only the required `/storage/logs` and `/storage/experiments` parents.
3. Move each source directory to its exact `/storage` destination, preserving contents and metadata as far as `mv` permits.
4. Create symlinks at the original brain-image paths pointing to the new absolute destinations.
5. Locate the diffusionregpose checkout; if the requested logs directory exists, move it to `/storage/logs/diffusionregpose` and link the original path. Do not create a fabricated link for a missing checkout.
6. Verify symlink resolution, directory sizes, representative files, and that the source filesystem has reclaimed the expected space.
7. Update README storage/layout guidance and append concise migration evidence to `.log` and this plan.

Feature list:
- Storage-backed brain-image experiment logs.
- Storage-backed brain-image experiment artifacts.
- Backward-compatible repository paths through symlinks.
- Storage-backed diffusionregpose logs when the checkout is present.

Function/interface contracts:
- Existing code continues to use `logs/` and `experiments/` without changes.
- Canonical data locations become `/storage/logs/brain-image-implementation` and `/storage/experiments/brain-image-implementation`.
- The assumed canonical diffusionregpose log location is `/storage/logs/diffusionregpose`; confirm against the actual checkout before moving it.

Edge cases:
- A running process could write during migration and invalidate size/file-count checks; migration must stop if relevant writers are found.
- Existing non-empty destinations must never be merged or overwritten implicitly.
- A missing diffusionregpose path is not an error in this checkout; report it and leave unrelated paths untouched.
- Symlink creation must not replace a real directory or file.
- If validation fails, remove only the newly created symlink and move the exact directory back to its original path.

Side effects:
- Approximately 54 GB will move from `/home` to `/storage`.
- Existing absolute paths and shell commands remain usable through symlinks.
- Directory inode/device identities will change because the data is relocated.

Tests to add:
- No production-code test is needed for the filesystem migration.
- Run shell-level assertions for source symlink state, destination existence, resolved paths, and matching file counts/byte totals.

Possible refactors or suggestions:
- Keep future large checkpoints, logs, and experiment outputs under `/storage`; avoid adding repository-specific symlink logic to Python code.

TODOs:
- Identify the actual diffusionregpose checkout before attempting its migration.
- Confirm whether `/storage/logs/diffusionregpose` is the desired canonical destination if that checkout is found elsewhere.

Notes / risks / suggestions:
- The requested brain-image paths are ordinary directories, not symlinks. The pose path `/home/gasparyanartur/dev/pose-graph-diff/external/diffusionregpose` is currently absent.
- `/home` is at 99% usage with about 5.8 GB free; `/storage` is at 74% usage with about 303 GB free.
- This plan intentionally uses explicit moves and symlinks; no recursive deletion or overwrite is authorized.

README updates:
- Add a short canonical storage-location note and state that the repository paths are compatibility symlinks.
- Do not add a long Slurm or operational section as part of this migration.

Implementation status:
- Moved the brain-image `logs` directory to `/storage/logs/brain-image-implementation` and `experiments` to `/storage/experiments/brain-image-implementation`.
- Replaced both original paths with absolute symlinks and verified they resolve to populated directories.
- The requested diffusionregpose checkout/path is absent, so it was left untouched; no fabricated link was created.
- `/home` free space increased from approximately 5.8 GB to approximately 60 GB after the migration.
- The cross-filesystem move printed transient `mv` warnings for entries that had already disappeared during traversal; final source paths are symlinks and canonical destinations contain the migrated trees. A deeper file-count reconciliation remains a follow-up if required.
- During caption resumption, the existing Gemma smoke session exposed a stale `img_dir` assumption. The caption script now uses the dataset's configured `data_path` directly, and the Gemma default output is versioned separately from the legacy `local.jsonl`.
- The dataset inspection showed that `things-eeg2` stores images under `imgs/`; the script now uses the configured `img_dir` when present and otherwise honors the dataset class default `imgs`.

Continuation status:
- Gemma smoke completed successfully and wrote one caption record for the `aardvark` image in 2 minutes 20 seconds.
- Full Gemma train+test generation is active in tmux session `gemma_caption_smoke`, writing resumable records to `data/things-eeg2/captions/gemma-4-E2B-it.jsonl` with provenance in the adjacent YAML file.
- The full run has loaded the offloaded model and enumerated 16,540 training images; its first inference is in progress. It uses batch size 1 and `device_map=auto`.

[2026-09-02 20:05] Switch caption regeneration to Qwen3.5 0.8B

Request summary:
- Cancel the impractically slow Gemma caption regeneration.
- Try the multimodal instruction-tuned `Qwen/Qwen3.5-0.8B` checkpoint instead.
- Preserve the partial Gemma and older Qwen artifacts.

Implementation steps:
1. Stop only the active Gemma caption process and verify its partial JSONL remains intact.
2. Reuse the multimodal `AutoModelForMultimodalLM` and chat-template path for Qwen3.5.
3. Write Qwen3.5 output to a separate versioned JSONL/YAML pair.
4. Run a one-image smoke test, then start the full train+test generation only after the smoke test writes a valid caption.

Feature list:
- Qwen3.5 0.8B multimodal caption generation.
- Separate versioned output and provenance artifacts.

Function/interface contracts:
- Model IDs beginning with `Qwen/Qwen3.5` use the same image chat-template interface as Gemma 4.
- Existing Qwen 2.5 processing remains available for older model IDs.
- Output paths are model-specific and resumable through existing-path detection.

Edge cases:
- Qwen3.5 0.8B can enter thinking loops; disable thinking and cap generation length for captioning.
- The competing diffusionregpose process still occupies GPU memory; automatic placement may be needed until it finishes.
- A failed smoke test must not overwrite any existing caption artifact.

Tests to add:
- Smoke-test model loading, image processing, generation, and one-record JSONL serialization.

TODOs:
- Complete the smoke test and launch full generation if it succeeds.

Notes / risks / suggestions:
- The official Qwen3.5 model card documents `AutoModelForMultimodalLM` and `processor.apply_chat_template` for this checkpoint.
- Gemma generation was cancelled after producing four full-run records; those records remain preserved.

Implementation status:
- Cancelled the Gemma process cleanly; its four generated records remain in `gemma-4-E2B-it.jsonl`.
- Qwen3.5 smoke loaded all 473 shards and generated one caption in approximately 22 seconds.
- Full Qwen3.5 train+test generation is active in tmux `gemma_caption_smoke`, with one full-run record written to `qwen3.5-0.8b.jsonl`.
- The active Qwen3.5 process uses approximately 2 GB GPU memory with the competing diffusionregpose job still present; observed throughput is approximately 24 seconds per image.

[2026-09-02 20:15] Increase Qwen3.5 caption batch size and make CUDA placement explicit

Request summary:
- Confirm whether the Qwen3.5 run is actually CPU-offloaded.
- Increase the caption batch size modestly to reduce the approximately 4.6-day estimate.

Implementation steps:
1. Stop only the active Qwen3.5 caption process, preserving its 125 generated records.
2. Set the maintained caption configuration to batch size 4 and `device_map: null`.
3. Move the loaded caption model to the selected CUDA device when no device map is configured.
4. Run a batch-4 smoke test and inspect memory/throughput.
5. Resume the versioned full JSONL, relying on existing-path skipping for already generated records.

Feature list:
- Explicit full-CUDA placement for the small Qwen3.5 model.
- Batch-size-4 caption generation.

Function/interface contracts:
- `device_map: null` means load normally and explicitly move the model to `device`.
- `device_map: auto` remains available for larger multimodal models such as Gemma.
- Existing versioned caption files remain append-only/resumable.

Edge cases:
- Batch size 4 must fit alongside the active diffusionregpose process.
- If the smoke test OOMs, revert only the batch override to 1 while retaining explicit CUDA placement.
- The first batch-4 smoke was blocked at Pydantic validation because `device_map` was typed as non-nullable; the local config model now accepts `str | None`.

Tests to add:
- Batch-4 smoke test must generate one JSONL record and exit successfully.

TODOs:
- Measure the new full-run throughput and update the ETA.

Notes / risks / suggestions:
- `device_map=auto` was not causing meaningful offloading: Qwen3.5 occupied approximately 2 GB GPU memory while the competing process occupied approximately 12.6 GB.

README updates:
- Update the caption configuration description after the batch-4 smoke test is verified.

Verification status:
- The initial batch-4 full run stopped after 129 records on variable-length tokenizer input; all 129 records pass JSON parsing, unique-path, existing-path, non-empty-caption, and label/path consistency checks.
- Mixed-label batch-4 smoke passed with 16/16 records and exited cleanly after explicit padding was added.
- The full Qwen3.5 run has been resumed in tmux `gemma_caption_smoke`; it is loading the model and will continue from the 129 preserved records.
- README now records Qwen3.5, batch size 4, and explicit CUDA placement.

Completion status:
- Full Qwen3.5 captioning completed successfully with 16,540 train and 200 test records (16,740 total).
- Final validation passed: 16,740 unique existing image paths, zero JSON parse errors, zero empty captions, and exact expected schema.
- Provenance records `Qwen/Qwen3.5-0.8B`, batch size 4, CUDA placement, and the train/test split configuration.

[2026-09-03] Rebuild T5 text artifacts from Qwen3.5 captions

Request summary:
- Use the completed Qwen3.5 captions as input for the downstream text-alignment artifacts.
- Keep the maintained target as `t5_base`; Qwen is only the caption generator.
- Preserve old caption-derived caches and statistics through versioned output directories.

Implementation steps:
1. Configure text embedding generation for `t5_base`, Qwen3.5 captions, and `tensorcache/qwen3.5-0.8b`.
2. Configure text statistics generation for `t5_base`, the same cache, and `statistics/qwen3.5-0.8b`.
3. Point `train_eeg_align_text` at the versioned cache and statistics paths.
4. Generate embeddings and statistics sequentially in tmux, with the environment activated by the agent.
5. Validate all train/test captions, embeddings, dimensions, and statistics before any text-alignment training.

Feature list:
- Versioned T5 embeddings derived from Qwen3.5 captions.
- Matching versioned T5 statistics.
- Text-alignment config provenance tied to those artifacts.

Function/interface contracts:
- Caption path: `data/things-eeg2/captions/qwen3.5-0.8b.jsonl`.
- Embedding cache: `tensorcache/qwen3.5-0.8b`.
- Statistics root: `statistics/qwen3.5-0.8b`.
- Alignment target: `t5_base`, 768 dimensions.

Edge cases:
- Do not overwrite old `tensorcache` or `statistics` artifacts.
- Validate that every image path has exactly one caption and one T5 embedding.
- Confirm the statistics generator consumes the versioned cache and writes both train/test files.

Tests to add:
- Run the existing artifact validator against the Qwen3.5 caption file and versioned T5 cache.
- Check representative embedding shapes and statistics files before training.

TODOs:
- Execute embedding generation and statistics generation.
- Decide whether to run a text-alignment training smoke test after validation.

Implementation note:
- The first embedding launch failed before execution because `generate_text_embeddings.py` imported the full EEG/diffusion dataset union, triggering a `diffusers`/Transformers `HybridCache` incompatibility. The script now enumerates configured image paths directly, matching the caption generator's lightweight path.
- The statistics launch exposed the same union import through `EEGDataModule`; `generate_stats.py` now uses the concrete `ThingsEEG2DatasetConfig` for the maintained Things-EEG2 text-statistics command while still loading EEG and cached embeddings.
- The retry also exposed an image-side encoder registry import in `generate_stats.py`; replaced it with the text-only `TEXT_ENCODER_DIM` registry, leaving the stats algorithm unchanged.

README updates:
- Document the Qwen3.5 caption input and versioned T5 cache/statistics paths.

Validation note:
- The artifact validator API accepted a model subset but its CLI did not; added `--models` so versioned T5-only artifacts can be validated without requiring unrelated legacy caches.

Completion status:
- Generated 16,540 train and 200 test `t5_base` embeddings from the Qwen3.5 captions under `tensorcache/qwen3.5-0.8b`.
- Generated matching train/test EEG and T5 statistics under `statistics/qwen3.5-0.8b`.
- Validator passed with `--models t5_base --check-tensor-shapes`: 16,740 captions, zero missing paths/embeddings, and matching tensor/statistics dimensions.

Text-alignment smoke status:
- The first training config composition is blocked before Hydra execution because the installed Qwen3.5-compatible Transformers development build does not export `HybridCache`, while the repository's locked `peft 0.17.1` and Diffusers import path require it.
- No training process was started and no text-alignment experiment artifacts were created.
- Resolution requires either a compatible package set/isolated training environment or a deliberate import isolation refactor for the alignment model; do not change package versions implicitly.

Update:
- Upgraded PEFT to `0.20.0`; Diffusers and training-module imports now succeed.
- The first smoke after that upgrade exposed a duplicate `trainer` YAML mapping introduced while adding the versioned cache path. Merged `trainer.cache_dir` into the existing block; no training artifacts were created.

Requirements update:
- Declared `peft>=0.20.0` and pinned Transformers to the verified Qwen3.5-compatible commit `dd35e077078d9aebbdbc8f64a138a5951aa79ffc` so a fresh environment does not recreate the `HybridCache` conflict.

Completion status:
- Text-alignment smoke training completed for one epoch on 1% of the training data with batch size 32.
- Separate evaluation loaded `last.ckpt` and wrote `test_metrics.csv` plus `evaluation_config.yaml`.
- Smoke metrics: `brain_acc=0.01`, `image_acc=0.0`, `sim=0.0486275`.
- `uv lock --check` passed; training imports pass with PEFT 0.20.0, Transformers 5.16.0.dev0, and Diffusers 0.35.2.
- Focused text-artifact tests pass: 2 tests.

Follow-up:
- The full batch-4 run stopped on a mixed-label batch because the Qwen3.5 chat-template tokenizer received variable-length text without explicit padding. Added `padding=True` at the shared multimodal processor boundary; the 129 valid records already written remain preserved.

README updates:
- Update the current regeneration model and output path after the Qwen3.5 smoke test is verified.
[2026-09-04 00:00] Launch full Qwen3.5-caption T5 EEG alignment training

Request summary:
- Train the maintained text-alignment experiment now that Qwen3.5 captions, T5 embeddings, and statistics have been generated and validated.
- Use the pure alignment contract: no EEG augmentation, symmetric InfoNCE, no MSE, and accuracy-based checkpoint selection.

Implementation steps:
- Update `src/brain_image/configs/train_eeg_align_text.yaml` to inherit the no-augmentation preset, explicitly configure pure symmetric InfoNCE, use batch size 32 and four workers, and monitor `val/align/top1` in max mode.
- Compose the config and inspect the effective values before launch.
- Launch the full 200-epoch run sequentially in a new tmux session with `BRAIN_IMAGE_TENSORCACHE_MAXSIZE=1024`, the environment activated by the agent, and W&B disabled.
- After training completes, run `scripts/evaluation/test_eeg.py` against the best `val/align/top1` checkpoint and record both `brain_acc` and `image_acc` from the CSV.

Feature list:
- Reproducible text-alignment training run using versioned Qwen3.5-derived T5 artifacts.
- Best-checkpoint selection by validation alignment top-1 accuracy.
- Training TensorBoard and separate evaluation CSV/YAML artifacts.

Function/interface contracts:
- Config: `train_eeg_align_text`.
- Target encoder: `t5_base`.
- Cache: `tensorcache/qwen3.5-0.8b`; statistics: `statistics/qwen3.5-0.8b`.
- Evaluation selection: `--checkpoint_selection max --checkpoint_metric val/align/top1`.

Edge cases:
- A full run is long; interruption must leave resumable Lightning artifacts.
- Validation checkpoints are created every five epochs, so the one-epoch smoke's `last.ckpt` is not representative of best-checkpoint selection.
- Text-alignment evaluation must preserve both retrieval directions and write CSV/YAML without adding TensorBoard output.

Tests to add:
- Validate the composed config has no augmentation, no MSE, InfoNCE, batch size 32, four workers, and max accuracy checkpointing.
- Run the existing text-artifact validator before training.
- Run the separate evaluator after training and verify the expected CSV columns.

TODOs:
- Record the final run path, selected checkpoint, and measured metrics after completion.

Status:
- Effective config verified and full training launched sequentially in tmux session `eeg_text_full`.
- Run root: `experiments/eeg_alignment_text/20260904_002351`.
- Training completed by early stopping at epoch 129.
- Best checkpoint selected by `val/align/top1`: `epoch_0029-val_align_top1_0.5250.ckpt`.
- Separate evaluation completed and wrote `test_metrics.csv` and `evaluation_config.yaml`.
- Test metrics: `brain_acc=0.215`, `image_acc=0.170`, `sim=0.21608`.

Notes / risks / suggestions:
- The full run is intentionally sequential and isolated in tmux; no parallel sweep is being launched.
- The missing `docs/project.md` and `docs/architecture.md` remain a repository documentation debt from the earlier handover work.

README updates:
- Add or correct the maintained text-alignment launch/evaluation command after the run path is known.
[2026-09-04 09:00] Document text-target alignment progress

Request summary:
- Write up the completed Qwen3.5-caption to T5 text-target alignment progress in a clean standalone report notebook under `notebooks/`.
- Make the report concrete and reproducible, with the maintained objective, artifact provenance, training/evaluation paths, and measured metrics.

Implementation steps:
- Inspect the current text-alignment run metadata, effective configuration, training metrics, evaluation CSV, and validated caption/cache/statistics artifacts.
- Create an executed notebook, likely `notebooks/text_alignment_report.ipynb`, with sections for pipeline, protocol, artifact validation, run results, and conclusions.
- Include links to the maintained config, training/evaluation scripts, artifact validator, and saved CSV/YAML outputs.
- Execute the notebook against the saved artifacts and verify that its tables and plots render without requiring GPU training.
- Link the notebook from README if that is not already present.

Feature list:
- Artifact-backed text-alignment progress report.
- Reproducible current-run metric table with `brain_acc`, `image_acc`, and similarity.
- Explicit statement of the intended pure-InfoNCE protocol.

Function/interface contracts:
- Current run: `experiments/eeg_alignment_text/20260904_002351/train_eeg-20260904_002425/version_0`.
- Current evaluation source: `test/test_metrics.csv` and `test/evaluation_config.yaml`.
- Notebook must be executable with repository artifacts only and must not launch training or require GPU.

Edge cases:
- Evaluation is stored as CSV/YAML and must be read without rewriting the saved artifacts.
- The report must not imply that any reconstruction metric is part of this alignment-only result.

Tests to add:
- Execute the notebook with `jupyter nbconvert --execute` or the repository's available notebook runner.
- Validate that the report contains the expected current metrics and historical T5 comparison row.

TODOs:
- No remaining implementation TODOs for this report.

Completion status:
- Created and executed `notebooks/text_alignment_report.ipynb` against the saved caption, cache, statistics, checkpoint, and CSV/YAML evaluation artifacts.
- The notebook contains seven cells, four executed code cells, and six rendered outputs; no GPU work was performed.
- README now links the standalone report and documents the current Qwen3.5 caption path.

Notes / risks / suggestions:
- The current result is `brain_acc=0.215`, `image_acc=0.170`, `sim=0.21608`.

README updates:
- Add a link and one-sentence description for the text-alignment progress report.
[2026-09-04 10:00] Run joint EEG alignment and diffusion-prior experiment

Request summary:
- Recreate the joint alignment-plus-reconstruction experiment using the existing `train_eeg_alignprior.yaml` path.
- Use the finished EEG alignment model as the initialization where the existing configuration supports it, then train and evaluate the joint model sequentially.

Implementation steps:
- Audit `train_eeg_alignprior.yaml`, `train_eeg.py`, `EEGAlignmentModel`, and `test_eeg.py` to verify the effective alignment target, prior target/statistics, loss factors, initialization checkpoint, validation cadence, and checkpoint monitor.
- Confirm the intended comparison scope: joint alignment and prior training, with reconstruction validation kept separate/cheap during training and full reconstruction metrics produced by evaluation.
- Add or update only the dedicated joint local launcher if the existing prior launcher cannot express the joint config; keep environment activation outside the launcher.
- Run a one-epoch or minimal-data smoke test with batch size 32, four workers, cache maxsize 1024, W&B disabled, and explicit checkpoint initialization. Verify checkpoint creation and CSV/YAML evaluation.
- Launch the full joint run sequentially in a new tmux session after the smoke passes.
- Evaluate the best checkpoint using the configured validation prior metric and record alignment (`brain_acc`, `image_acc`, `sim`) plus reconstruction metrics.
- Write a standalone report notebook and update README with the exact command, artifact paths, and measured results.

Feature list:
- Reproducible joint alignment/prior training path.
- Explicit reuse of the finished EEG alignment checkpoint.
- Separate training TensorBoard and evaluation CSV/YAML/reconstruction artifacts.
- Artifact-backed report comparing joint results with the already documented prior-only baseline.

Function/interface contracts:
- Config: `src/brain_image/configs/train_eeg_alignprior.yaml`.
- Training entrypoint: `scripts/training/train_eeg.py` unless the audit proves a dedicated wrapper is required.
- Evaluation entrypoint: `scripts/evaluation/test_eeg.py`.
- Training defaults: batch size 32, four workers, cache maxsize 1024, W&B disabled for local execution.
- Evaluation must select the best checkpoint using the config's monitored validation metric, not `last.ckpt`.

Edge cases:
- The joint config may not initialize from the finished alignment checkpoint unless `checkpoint_path`/`eeg_encoder_path` is explicitly wired.
- Alignment and prior targets require distinct encoder/statistics keys; changing either target requires matching caches and stats.
- The joint objective may include alignment, prior prediction, and prior simulation losses; record all effective factors rather than assuming prior-only behavior.
- Full reconstruction evaluation is expensive and may require optional metric networks; a smoke may use explicit reduced metrics/reconstruction indices.
- An interrupted tmux run must leave its checkpoint and TensorBoard artifacts resumable.

Tests to add:
- Config-composition assertions for joint `do_align`/`do_recon`, initialization checkpoint, target/statistics keys, batch/worker settings, and checkpoint monitor.
- Minimal smoke verification that the selected alignment checkpoint loads and both alignment/prior paths execute.
- Regression check that evaluation selects the intended best checkpoint and writes expected CSV columns.

TODOs:
- Complete the config audit and decide whether a small config/launcher correction is required before the smoke.
- Record the final run, selected checkpoint, and measured metrics after completion.

Status:
- Joint config corrected and composed successfully.
- Smoke training completed with both alignment and prior paths; monitored checkpoint `epoch_0000-eval_val_prior_pred_cos_0.5996.ckpt` was created.
- Smoke evaluation passed with SSIM-only metrics after pinning `setuptools<81` to restore `pkg_resources`; the optional CLIP metric had previously failed only because of that environment mismatch.
- Dedicated sequential launcher added at `scripts/run_eeg_alignprior.sh`.
- Full run launched in tmux session `eeg_alignprior_full` under `experiments/eeg_alignprior/20260904_100813`.
- Full joint run completed successfully and the launcher evaluated the selected checkpoint.
- Selected checkpoint: `epoch_0409-eval_val_prior_pred_cos_0.6420.ckpt`.
- Test metrics: alignment `brain_acc=0.595`, `image_acc=0.515`, `sim=0.2642`; prior cosine `0.6420`; PixCorr `0.1811`; SSIM `0.3157`; AlexNet-2 `0.9444`; AlexNet-5 `0.9167`; Inception `0.8056`; CLIP `0.8472`; EfficientNet `0.8912`; SwAV `0.5645`.

Notes / risks / suggestions:
- This is a new joint experiment, not a re-run of the prior-only baseline.
- Preserve the existing prior-only artifacts and report unchanged.
- The repository has no `docs/project.md` or `docs/architecture.md`; do not block the experiment on that unrelated documentation debt.

README updates:
- Add the joint launch/evaluation command and link its standalone report after verification.
[2026-09-04 15:00] Add joint alignment-prior results to prior report

Request summary:
- Extend the existing prior/reconstruction report notebook with the completed joint EEG alignment + diffusion-prior experiment.
- Keep the prior-only baseline and joint result clearly separated.

Implementation steps:
- Inspect `notebooks/eeg_prior_reconstruction_report.ipynb` and identify its result tables and conclusion cells.
- Add the joint run path, selected checkpoint, effective protocol, and measured alignment/prior/reconstruction metrics using the saved CSV/YAML artifacts.
- Add a compact comparison table between prior-only and joint runs, clearly labeling metrics that are available for each track.
- Execute the updated notebook against saved artifacts only; do not retrain or launch GPU work.
- Update README wording/link if the prior report description currently implies it covers only the prior-only run.

Feature list:
- Combined prior/reconstruction report covering prior-only and joint experiments.
- Artifact-backed joint metrics and checkpoint provenance.
- Explicit distinction between alignment retrieval metrics and reconstruction metrics.

Function/interface contracts:
- Existing report: `notebooks/eeg_prior_reconstruction_report.ipynb`.
- Joint run: `experiments/eeg_alignprior/20260904_100813/train_eeg-20260904_100911/version_0`.
- Joint evaluation source: `test/test_metrics.csv` and `test/evaluation_config.yaml`.
- Notebook execution must not invoke training, evaluation, or model loading.

Edge cases:
- Prior-only and joint reports use different run paths and checkpoint criteria; retain both labels.
- Joint alignment metrics (`brain_acc`, `image_acc`, `sim`) are not reconstruction metrics.
- The report must preserve the existing prior-only values unchanged.

Tests to add:
- Execute the notebook with the available notebook runner.
- Verify the rendered report contains the joint checkpoint and key metrics.

TODOs:
- No remaining implementation TODOs for this report update.

Completion status:
- Updated and executed `notebooks/eeg_prior_reconstruction_report.ipynb` with the prior-only and joint alignment+prior runs.
- Added the joint protocol, checkpoint, alignment metrics, prior metrics, comparison table, and reconstruction visualizations.
- Notebook execution succeeded from saved artifacts only; no training or evaluation was launched.
- README now documents the joint launcher and describes the combined report.

Notes / risks / suggestions:
- Joint run selected `epoch_0409-eval_val_prior_pred_cos_0.6420.ckpt`.
- Joint test metrics include prior cosine `0.6420`, PixCorr `0.1811`, SSIM `0.3157`, and alignment image top-1 `0.515`.

README updates:
- Clarify that the prior/reconstruction report now includes both prior-only and joint results.
[2026-09-04 16:00] Prepare low-level reconstruction at 128px

Request summary:
- Move to the low-level reconstruction track after completing the joint alignment/prior experiment.
- Use 128x128 image resolution and a small memory-conscious batch size.

Implementation steps:
- Audit `scripts/training/train_low_level.py`, `src/brain_image/model/low_level.py`, `src/brain_image/configs/train_low_level.yaml`, `train_low_level_dreamsim.yaml`, and the current data/evaluation interfaces.
- Identify the stale interfaces and the exact image-size controls used by the low-level model, VAE decoder, dataloader, and evaluator.
- Update the maintained low-level baseline config and entrypoint to use 128px images, batch size 32 as the initial conservative default, four workers, cache maxsize 1024, and separate evaluation artifacts where compatible.
- Add or update a simple local train-then-evaluate launcher without embedding environment activation or SLURM behavior.
- Run a minimal smoke test that verifies tensor/image dimensions, checkpoint creation, TensorBoard output, and evaluation CSV/YAML artifacts.
- Run the full low-level baseline only after the smoke passes; preserve the DreamSim variant for a later controlled comparison.
- Document the measured baseline in a standalone notebook and update README.

Feature list:
- Maintained low-level reconstruction baseline at 128x128.
- Small-batch local execution path.
- Separate training and evaluation outputs.
- Explicitly deferred DreamSim comparison.

Function/interface contracts:
- Config: `src/brain_image/configs/train_low_level.yaml`.
- Training entrypoint: existing low-level script if it can be repaired with the current model API; otherwise a small replacement entrypoint.
- Evaluation: existing evaluator if compatible, otherwise a low-level-specific evaluator that writes CSV/YAML.
- Default image size: 128.
- Initial batch size: 32 for train/validation/test; lower if the smoke demonstrates memory pressure.

Edge cases:
- The old low-level training script may use obsolete dataset/model constructor arguments.
- Image-size assumptions may be embedded in latent shape, VAE decoding, transforms, or metric code.
- The low-level path may require a different target cache/statistics set than the prior path.
- Optional reconstruction metrics may require GPU memory beyond the training budget.
- Interrupted runs must leave recoverable checkpoints and must not overwrite prior/joint artifacts.

Tests to add:
- Config composition assertions for image size, batch sizes, workers, target latent dimensions, and checkpointing.
- Minimal forward/shape test for the low-level model at 128px.
- Smoke test for training checkpoint/TensorBoard and separate CSV/YAML evaluation artifacts.

TODOs:
- Launch the full 128px low-level baseline and record its selected checkpoint and reconstruction metrics.

Notes / risks / suggestions:
- Batch size 32 is an explicit working assumption for “small”; reduce it if the actual low-level memory profile requires it.
- Do not start the DreamSim comparison until the ordinary low-level baseline is working.

README updates:
- Added the 128px low-level launch command and artifact layout. Add the measured baseline/report link after the full run.

Completion status so far:
- Generated the missing `ip_sdxl_turbo_128` train/test latent caches and statistics.
- Repaired the low-level dataset/config constructor interfaces and injected dataset dimensions into ATMS.
- Verified one-epoch training, checkpoint creation, TensorBoard output, and separate CSV/YAML evaluation on a 1% smoke run.
- The full baseline remains pending; DreamSim remains deferred.
- Full baseline launched sequentially in tmux session `low_level_full`; evaluation will run automatically after training exits.

[2026-09-05 09:00] Prevent low-level training OOM from expensive metrics

Request summary:
- The full 128px low-level run exhausted GPU memory during in-training CLIP validation. Disable expensive full reconstruction metrics during training while retaining them in standalone evaluation.

Feature list:
- Training performs only the latent reconstruction loss and lightweight validation needed for checkpoint selection.
- Standalone `scripts/evaluation/test_low_level.py` remains responsible for SSIM, CLIP, and DreamSim metrics.
- The low-level full run can resume or restart without changing the target, model, or loss recipe.

Implementation steps:
- Add an explicit low-level config switch controlling full reconstruction evaluation during validation, defaulting to disabled.
- Guard `LowLevelModule.validation_step` with that switch; keep `test_step` full evaluation enabled for direct/manual model testing.
- Set the maintained low-level training config to disable validation-time reconstruction metrics.
- Add a focused regression test or config assertion, run a short smoke training/evaluation, and restart the full baseline only after verification.
- Record the failed run and new launch/evaluation behavior in README, `.plan`, and `.log`.

Function/interface contracts:
- New model/config flag: `run_validation_reconstruction_metrics: bool`, default `false` for the maintained low-level baseline.
- Test-time evaluation remains unchanged and writes `test_metrics.csv` through the standalone evaluator.

Edge cases:
- `test_step` must still produce reconstruction metrics when called by the standalone evaluator.
- Existing checkpoints remain valid; the change affects validation scheduling only.
- If the failed process still holds GPU memory, terminate only that stale low-level process before relaunching.

Side effects:
- Training TensorBoard will no longer contain periodic full-image metric scalars; those metrics move exclusively to post-hoc evaluation.
- A fresh run directory is required to avoid mixing logs from the failed run and the corrected run.

Tests to add:
- Assert the maintained config disables validation reconstruction metrics.
- Smoke-test one epoch and verify checkpoint plus standalone CSV output.

TODOs:
- Add the measured low-level baseline and visualization to a standalone report notebook.

Notes / risks / suggestions:
- The previous run reached at least epoch 139 and saved `epoch_0089-val_loss_0.5974.ckpt`; it failed in `get_metric_clip` during validation, not in the latent loss.

README updates:
- Document that low-level training uses latent-loss validation only and that image metrics are post-hoc.

[2026-09-05 10:30] Use batch-one external low-level evaluation

Request summary:
- Keep reconstruction evaluation outside training and use a very small evaluation batch size; default to one.

Implementation steps:
- Add a `--batch_size` option to `scripts/evaluation/test_low_level.py`, defaulting to 1.
- Apply it to train/validation/test dataloaders and low-level VAE decode batching used during evaluation.
- Rerun the best checkpoint and verify the CSV/YAML records the effective batch-one configuration.

Feature list:
- Low-memory external evaluation with unchanged metric definitions.

Function/interface contracts:
- `test_low_level.py RUN_PATH --batch_size N`; default `N=1`.

Edge cases:
- Batch one reduces peak memory but does not change the full-test metric denominator.
- The test split remains the only split evaluated by the standalone script.

Tests to add:
- CLI/config smoke assertion for effective batch size one and successful CSV output.

TODOs:
- Add the measured low-level baseline and visualization to a standalone report notebook.

Notes / risks / suggestions:
- Full metric feature extraction still compares all 200 test examples; batch one primarily bounds dataloader and VAE decode memory.

Completion status:
- External evaluation rerun completed on the explicit best checkpoint with batch size 1 for all loaders and VAE decoding.
- CSV/YAML artifacts were written successfully; metrics matched the prior full evaluation.

[2026-09-05 11:30] Run the complete low-level perceptual test suite

Request summary:
- Evaluate the trained low-level pipeline on the full test set using the repository's complete perceptual/reconstruction metrics, externally, with batch size 1.

Implementation steps:
- Expose the standard `METRIC_LOOKUP` suite through `LowLevelConfig.test_recon_metrics`.
- Make low-level full evaluation call that suite on generated low-level reconstructions, while retaining DreamSim diagnostics.
- Run the explicit best checkpoint through `test_low_level.py --batch_size 1` and verify every metric is present in CSV/YAML.

Feature list:
- Complete low-level test-set perceptual metrics: PixCorr, SSIM, AlexNet-2/5, Inception, CLIP, EfficientNet, SwAV, and DreamSim aligned/unaligned.

Function/interface contracts:
- `test_low_level.py RUN_PATH --checkpoint_path BEST --batch_size 1` writes one CSV row per metric.

Edge cases:
- Feature networks may be CPU/GPU memory intensive; batch one bounds reconstruction decoding, while feature metrics still compare all 200 test examples.
- A missing optional metric dependency or model download must fail explicitly rather than silently omit a metric.

Tests to add:
- Smoke assertion that the configured metric list is passed to low-level full evaluation.
- Full test run must contain all requested metric names.

TODOs:
- None for the complete low-level perceptual evaluation. A future report notebook can visualize low-level reconstructions if requested.

Notes / risks / suggestions:
- The prior low-level evaluation only ran SSIM, CLIP, and DreamSim; this iteration corrects that incomplete deliverable.

README updates:
- Documented the complete low-level metric output and measured baseline after the run.

README updates:
- Document the batch-one default for standalone low-level evaluation.

Completion status:
- Full external evaluation completed on all 200 test examples with batch size 1.
- Explicit checkpoint: `experiments/low_level/20260905_025455/train_low_level-20260905_025524/version_0/checkpoints/epoch_0159-val_loss_0.6073.ckpt`.
- CSV contains PixCorr `0.2431227`, SSIM `0.3669125`, AlexNet-2 `0.7016081`, AlexNet-5 `0.6964573`, InceptionV3 `0.5478895`, CLIP `0.5161558`, EfficientNet `0.9738667`, SwAV `0.7549394`, and DreamSim aligned/unaligned `0.7877225/0.5578544`.

[2026-09-05 11:15] Create a dedicated low-level reconstruction report

Request summary:
- Create a standalone report notebook for the low-level EEG-to-image-latent pipeline and its completed perceptual evaluation.

Implementation steps:
- Inspect the maintained low-level config, training/evaluation scripts, checkpoint, CSV, and evaluation YAML to establish exact protocol and provenance.
- Add `notebooks/low_level_report.ipynb` with a concise pipeline description, training/evaluation artifact references, checkpoint selection, test-set metric table, and interpretation of metric directionality.
- Include a small visualization of representative low-level reconstructions against ground-truth test images if the run artifacts provide the necessary image tensors; otherwise document that the report is metric-only.
- Execute the notebook end to end in the project environment and verify that its displayed values match `test_metrics.csv`.

Feature list:
- Standalone low-level report with reproducible paths and measured test-set results.
- Complete metric table: loss, PixCorr, SSIM, AlexNet-2/5, InceptionV3, CLIP, EfficientNet, SwAV, and DreamSim diagnostics.

Function/interface contracts:
- Notebook reads the committed evaluation artifact rather than recomputing GPU metrics.
- Reported values must be loaded from the CSV and checkpoint/config provenance from the YAML/run path.

Edge cases:
- EfficientNet and SwAV use the repository's native metric direction and must not be mislabeled as higher-is-better.
- The report must distinguish the 128x128 low-level reconstruction from the later diffusion-prior pipeline.
- Missing image artifacts must not be silently fabricated; fall back to a metric-only report with an explicit note.

Side effects:
- Adds one maintained notebook under `notebooks/`; no training or evaluation behavior changes.

Tests to add:
- Execute all notebook code cells successfully.
- Assert the notebook-loaded metric values match the CSV values and the expected metric set is complete.

TODOs:
- None. The dedicated notebook was created and executed successfully.

Notes / risks / suggestions:
- The full metric evaluation already completed; this task documents those measured deliverables and does not launch another GPU job.
- Required `docs/project.md` and `docs/architecture.md` are missing from the checkout; no changes to those absent files are planned for this notebook-only task.

README updates:
- Add a link to the dedicated low-level report notebook next to the low-level pipeline instructions.

Completion status:
- Added and executed `notebooks/low_level_report.ipynb`.
- The notebook loads the evaluation CSV/YAML, verifies the 12 expected rows, confirms batch size 1 and checkpoint existence, and presents the complete metric table plus a summary plot.
- The run contains no saved reconstruction image files, so the report explicitly documents why it is metric-focused rather than inventing image visualizations.

[2026-09-05 11:30] Compare aligned and unaligned SynCLR low-level training losses

Request summary:
- Recreate the intended low-level perceptual-loss comparison. The existing completed low-level run is an L2-only control; it must not be treated as the perceptual-loss baseline.
- Train and evaluate matched unaligned and aligned SynCLR/DreamSim variants using the same maintained low-level pipeline and complete external metric suite.

Implementation steps:
- Preserve the completed L2-only run as the control and document its role accurately.
- Create or correct explicit configs for two variants: `use_perceptual_loss: true` with `perceptual_loss_model: unaligned_synclr_vitb16`, and `use_perceptual_loss: true` with `perceptual_loss_model: aligned_synclr_vitb16`.
- Keep all non-loss settings fixed: ATMS encoder, 128x128 SDXL-Turbo latent target, dataset split, optimizer/schedule, epoch budget, and cheap training validation. Use batch size 16 for perceptual-loss training, retain four workers, and use batch size 1 for external evaluation.
- Add/adjust a simple local launcher so each variant trains sequentially and then invokes the external evaluator on the selected best checkpoint.
- Evaluate both best checkpoints with batch size 1 using the complete metric suite and write independent CSV/YAML artifacts.
- Reuse the existing `tensorcache/{aligned,unaligned}_synclr_vitb16/{train,test}` target embeddings, add the selected target to each dataset sample, and compute only the predicted DreamSim embedding during training.
- Extend `notebooks/low_level_report.ipynb` with matched L2, unaligned SynCLR, and aligned SynCLR rows, protocol differences, and a comparison plot.

Feature list:
- Reproducible aligned-vs-unaligned SynCLR/DreamSim low-level loss comparison.
- Separate checkpoint and evaluation artifacts per loss.
- Reported comparison against the existing L2-only control.

Function/interface contracts:
- Training configs select only the loss variant; evaluation remains external through `scripts/evaluation/test_low_level.py`.
- Each run must identify its loss in its config, output directory, checkpoint provenance, and report table.

Edge cases:
- `perceptual_loss_model: unaligned_synclr_vitb16` and `aligned_synclr_vitb16` both select `DreamsimLoss` with different model wrappers.
- Perceptual loss still decodes predictions during training, but fixed target SynCLR embeddings can be loaded from the existing cache. Batch size 32 OOMed during the first VAE decode; batch size 16 is the current training setting.
- Best-checkpoint selection must use the configured validation criterion consistently across both variants, not the final epoch by default.

Side effects:
- Adds new experiment runs and evaluation artifacts; no existing L2 artifacts are overwritten.
- Updates the low-level report and README to distinguish the L2 control from perceptual-loss results.

Tests to add:
- Config composition assertions for unaligned and aligned SynCLR/DreamSim loss selection.
- Smoke-train each variant on a small data fraction and verify the selected loss is instantiated.
- Smoke-evaluate each checkpoint and verify the complete metric CSV is written.

TODOs:
- Monitor the fresh 100-epoch unaligned run, evaluate its selected checkpoint, and decide on the matched aligned run.

Completion status:
- Config composition passed for L2 and the corrected DreamSim mode.
- The previous LPIPS run was stopped before producing a result; no LPIPS comparison will be reported.
- DreamSim config now uses explicit `unaligned_synclr_vitb16` with batch size 16.
- Unaligned SynCLR batch-16 one-epoch smoke completed successfully.
- The first full unaligned run was stopped before producing a result; its partial artifacts are preserved and excluded from results.
- Existing aligned and unaligned SynCLR caches were verified to contain 16,540 train and 200 test embeddings each, with 768-dimensional tensors.
- The low-level DreamSim branch now consumes cached target embeddings and no longer loads raw ground-truth images for the perceptual branch.
- Cache-backed unaligned batch-16 smoke completed successfully; 11 training batches took about 27 seconds versus about 126 seconds before target caching.
- Fresh 100-epoch unaligned training is now active; external evaluation remains chained after training.

Notes / risks / suggestions:
- The prior `train_low_level_dreamsim.yaml` was misleadingly configured for LPIPS; it is now explicitly the unaligned SynCLR/DreamSim variant.
- The existing `tensorcache/{aligned,unaligned}_synclr_vitb16/{train,test}` artifacts contain the fixed target embeddings and will be reused after exact preprocessing parity is verified.
- Cached-target training retains the cached VAE latent for the L2 term; only the predicted image is decoded and embedded for the DreamSim term.
- The existing low-level report should be updated only after both matched perceptual runs have measured results.

README updates:
- Document the L2 control and aligned/unaligned SynCLR/DreamSim comparison once both runs are complete.

[2026-09-06 13:10] Fix and evaluate the completed unaligned DreamSim low-level run

Request summary:
- Fix the standalone low-level evaluator so it can load the completed checkpoint trained with `train_low_level_dreamsim`.
- Evaluate the selected best checkpoint and produce the complete test-set CSV metrics.

Implementation steps:
- Update `scripts/evaluation/test_low_level.py` to compose the same model configuration used by the run, rather than always composing baseline `train_low_level` with perceptual loss disabled.
- Preserve the evaluator's explicit CLI overrides for batch size, compilation, WandB, output directory, checkpoint path, and test-size limits.
- Ensure checkpoint selection still uses the configured `trainer.checkpoint_monitor` (`val/loss`, minimum), selecting epoch 89 for the current run unless an explicit checkpoint is supplied.
- Add a focused configuration/load regression check that confirms a DreamSim checkpoint can be instantiated and loaded without unexpected `perceptual_loss.*` keys.
- Run `scripts/evaluation/test_low_level.py` on the completed run with batch size 1 and verify the CSV contains the full configured metric set, including reconstruction metrics and aligned/unaligned DreamSim diagnostics.
- Record the selected checkpoint, effective evaluation configuration, metrics, and any evaluator behavior correction in `.plan`, `.log`, and the relevant README/report documentation if the user-facing invocation changed.

Feature list:
- Evaluator configuration parity between training and post-hoc evaluation.
- Successful batch-one evaluation of the completed 100-epoch unaligned SynCLR/DreamSim run.
- Reproducible CSV/YAML evaluation artifacts tied to the best validation checkpoint.

Function/interface contracts:
- `test_low_level.py RUN_PATH --batch_size 1` remains the public invocation.
- The evaluator must derive or accept the training config name; an explicit `--config_name` option is preferred if needed, with the current DreamSim run remaining simple to invoke.
- Checkpoint loading must be strict for the matching configuration; silently dropping `perceptual_loss.*` weights is not acceptable.
- Output remains `test/test_metrics.csv` and `test/evaluation_config.yaml` below the selected run version directory.

Edge cases:
- Baseline L2-only checkpoints must continue to evaluate without constructing a perceptual-loss module.
- A run may contain both `last.ckpt` and a monitored best checkpoint; default selection must remain the minimum `val/loss` checkpoint.
- Evaluation must use the test split and batch size 1, while retaining the external raw-image path needed for final image metrics.
- If the run's stored hyperparameters are sufficient to reconstruct the model, prefer them over fragile directory-name inference.

Side effects:
- The evaluator will load the frozen DreamSim model during evaluation, increasing startup time and memory relative to the L2-only evaluator.
- A new test artifact directory will be written under the existing completed run; training artifacts will not be modified.
- The current failed evaluation attempt must remain documented as a resolved configuration-parity failure.

Tests to add:
- A no-GPU/config-level regression test or smoke assertion that composes `train_low_level_dreamsim` and verifies `use_perceptual_loss=True` with `unaligned_synclr_vitb16`.
- A checkpoint-load smoke test using the completed checkpoint and matching config, with strict state-dict loading.
- Post-evaluation assertion that `test_metrics.csv` contains the expected metric names and `evaluation_config.yaml` records batch size 1 and the selected checkpoint.

TODOs:
- Update the low-level report and README only after the corrected evaluation produces final metrics.
- Decide whether to generalize evaluator config selection for future aligned SynCLR runs in this iteration or defer it after the current deliverable.

Notes / risks / suggestions:
- The training run completed through epoch 99; best validation loss is `0.811389` at epoch 89 and should be the default evaluation checkpoint.
- The current failure is in model construction/loading, not in the trained checkpoint or the training loss.
- `docs/project.md` and `docs/architecture.md` are missing from the checkout; do not create them as part of this narrowly scoped fix unless documentation requirements expand.

Analysis addendum after failure audit:
- The immediate failure is caused by `test_low_level.py` composing baseline `train_low_level`, which sets `use_perceptual_loss=false`; the checkpoint contains 150 `perceptual_loss.*` tensors from the trained unaligned SynCLR model.
- The checkpoint also contains compiled VAE keys such as `vae_encoder._orig_mod.*` and is approximately 838 MB because frozen VAE and perceptual-model weights are serialized. Evaluation must construct matching modules and retain strict loading; `strict=False` is explicitly disallowed as a workaround.
- `LowLevelModule` receives `compile=False` from the evaluator, but `load_vae_encoder()` currently uses its own default compilation behavior. This construction detail must be tested because it affects checkpoint key names.
- The launcher passes `LOW_LEVEL_CONFIG` only to `train_low_level.py`; it invokes the evaluator without a config name. The corrected interface must eliminate this train/eval config drift, preferably by reading the run's stored `hparams.yaml` as the source of truth, with an explicit config-name override only where necessary.
- `test_low_level.py` first runs `test_step`, which computes the configured latent/perceptual test loss, and then `run_full_eval()`, which reloads the test dataloader and computes raw-image reconstruction metrics plus aligned/unaligned DreamSim. Both stages need successful verification.
- Checkpoint selection by parsed `val_loss` filename selected epoch 89 (`0.8114`), matching the minimum validation value recorded in `metrics.csv` (`0.8113890290263062`); this selection should be asserted rather than inferred only from the directory listing.

Revised implementation steps:
- Add a small, testable configuration-loading helper for evaluation that reads the selected run version's `hparams.yaml` and reconstructs the stored model/dataset configuration, while applying only safe runtime overrides (compile flag, WandB, batch sizes, output path, and optional test limit).
- Preserve an explicit config-name/legacy fallback only for runs without `hparams.yaml`, and ensure baseline L2-only runs continue to work.
- Ensure runtime compilation choices do not alter state-dict key compatibility; either construct the VAE exactly as training did or make the compile choice explicit and verify strict loading against the completed checkpoint.
- Keep the evaluator's strict checkpoint load and fail with a concise diagnostic identifying config/checkpoint mismatches.
- Add tests for: stored-hparams reconstruction, baseline fallback, strict DreamSim checkpoint load, checkpoint selection, and preservation of the test metric configuration.
- Run the corrected evaluator on the epoch-89 checkpoint with batch size 1; verify both `test_step` loss logging and post-hoc reconstruction metrics complete.
- Only after successful evaluation, update the run log/README/report with the measured CSV results and corrected invocation.

README updates:
- Document the corrected evaluator invocation/config parity if the CLI contract changes.
- Add the measured final metrics only after successful evaluation.

Completion status:
- Added `--config_name` to `scripts/evaluation/test_low_level.py` and made `scripts/run_low_level.sh` forward the training config name to evaluation.
- Strictly loaded the completed unaligned DreamSim checkpoint with `train_low_level_dreamsim`; the previous `perceptual_loss.*` mismatch is resolved.
- Checkpoint selection was verified against `metrics.csv`: epoch 89, validation loss `0.811389`.
- Full batch-one evaluation completed on all 200 test examples. CSV metrics: test loss `0.8113877`, PixCorr `0.1976007`, SSIM `0.4171051`, AlexNet-2 `0.7315578`, AlexNet-5 `0.7117085`, InceptionV3 `0.5425125`, CLIP `0.5524623`, EfficientNet `0.9868256`, SwAV `0.7211292`, DreamSim aligned `0.7215754`, and DreamSim unaligned `0.5437707`.
- `evaluation_config.yaml` now records the actual automatically selected checkpoint path rather than `null`.
- Syntax checks passed; the full evaluation served as the strict checkpoint-load and metric-output smoke test.
- General automatic reconstruction from arbitrary stored `hparams.yaml` is deferred; the current maintained interface requires the explicit matching config name for direct evaluation and forwards it automatically through the local wrapper.

[2026-09-06 14:00] Train and evaluate the aligned SynCLR low-level variant

Request summary:
- Run the matched aligned-SynCLR perceptual-loss low-level experiment after the completed unaligned run.
- Keep the architecture, data, optimizer, epoch budget, cache strategy, and evaluation protocol fixed so the loss target is the only intended change.

Implementation steps:
- Add `src/brain_image/configs/train_low_level_aligned_dreamsim.yaml`, inheriting the maintained low-level DreamSim settings while selecting `aligned_synclr_vitb16`.
- Verify the aligned target cache exists for both train and test splits and that the resolved config uses batch size 16, four workers, 128px VAE targets, 100 epochs, and cached perceptual targets.
- Run a one-epoch/small-fraction aligned smoke test if the cache/config check does not already establish the path.
- Launch the full aligned run sequentially through `scripts/run_low_level.sh` with `LOW_LEVEL_CONFIG=train_low_level_aligned_dreamsim`, using the existing tensor-cache limit and no parallel experiment launch.
- Evaluate the automatically selected minimum-`val/loss` checkpoint with the corrected evaluator, explicit `--config_name=train_low_level_aligned_dreamsim`, and batch size 1.
- Verify strict checkpoint loading, complete CSV metrics, actual selected-checkpoint provenance, and comparison against the MSE and unaligned runs.

Feature list:
- Maintained aligned-SynCLR low-level training config.
- Matched aligned-versus-unaligned evaluation artifacts.
- Comparable full test-set perceptual metrics.

Function/interface contracts:
- `LOW_LEVEL_CONFIG=train_low_level_aligned_dreamsim ./scripts/run_low_level.sh <run_dir>` trains and evaluates sequentially.
- Direct evaluation uses `scripts/evaluation/test_low_level.py <run_dir> --config_name=train_low_level_aligned_dreamsim --batch_size 1`.
- The aligned run must use `perceptual_loss_model=aligned_synclr_vitb16` and retain strict checkpoint loading.

Edge cases:
- The aligned cache must be used for the training target; accidentally reusing the unaligned cache would invalidate the comparison.
- The evaluator must select the aligned run's own best checkpoint, not the unaligned or MSE artifact.
- GPU memory remains constrained by the unrelated diffusionregpose process; batch size 16 is the approved training setting and evaluation remains batch 1.

Side effects:
- Adds a new aligned experiment directory and large checkpoint artifacts.
- Adds one maintained config; no existing run or cache is overwritten.
- TensorBoard is produced for the aligned training run; CSV/YAML are produced by separate evaluation.

Tests to add:
- Resolve the aligned config and assert model/loss/cache names and fixed training parameters.
- Verify aligned cache train/test files are present and have the expected 768-dimensional embeddings.
- Smoke-load the aligned checkpoint strictly and assert the evaluation CSV contains the complete metric set.

TODOs:
- Update the low-level report notebook with the aligned row and matched comparison after evaluation.

Notes / risks / suggestions:
- The unaligned run's measured metrics are already available as the direct comparator.
- No algorithmic changes are planned; only the cached aligned SynCLR target changes.

README updates:
- Document the aligned config and matched comparison command after the run is verified.

Progress:
- Added `train_low_level_aligned_dreamsim.yaml` inheriting the unaligned maintained settings and overriding only `perceptual_loss_model` to `aligned_synclr_vitb16`.
- Resolved-config check passed: 100 epochs, batch size 16 for train/validation/test, four workers, `ip_sdxl_turbo_128`, and aligned loss target.
- Verified aligned cache: 16,540 train tensors and 200 test tensors, each shape `(768,)`.
- One-epoch 1% smoke training completed successfully with aligned cached targets.
- Full sequential run launched in tmux session `low_level_aligned_full`; post-training evaluation is chained through the corrected config-aware evaluator.

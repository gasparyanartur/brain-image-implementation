defaults:
  - train_low_level
  - _self_

model:
  use_perceptual_loss: true
  #perceptual_loss_model: "aligned_synclr_vitb16"
  perceptual_loss_model: "lpips"
  eval_batch_size: 32

dataset:
  batch_size: 32
  val_batch_size: 64
  test_batch_size: 64
# Embedding generation configuration

# Default components
defaults:
  - base
  - _self_

model_names: 
- aligned_synclr_vitb16
- unaligned_synclr_vitb16
- clip_vith14  

# Generation settings
batch_size: 128
splits:
  - train
  - test

models_path: models
dtype: float32
device: null
download_weights: true
cache_dir: tensorcache
img_dir: data/things-eeg2/imgs
compile: true
# Embedding generation configuration

# Default components
defaults:
  - dataset: eeg_dataset
  - _self_

# Generation settings
batch_size: 256
models:
  - unaligned_synclr_16
  - aligned_synclr_16
  - unaligned_clip_32
  - aligned_clip_32
splits:
  - train
  - test
img_size: [224, 224]
models_path: models
dtype: float32
device: null
output_dir: null